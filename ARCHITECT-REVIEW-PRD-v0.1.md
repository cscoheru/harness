# Fish Harness PRD v0.1 架构师审验报告

> 审验日期：2026-08-29
> 审验对象：`PRD-v0.1.md`
> 审验结论：**有条件不通过，修订后复审**
> 本文用途：交给 Claude Code 修订 PRD，不授权开始编码、部署或修改服务器

---

## 1. 给 Claude Code 的执行指令

请读取本文件和同目录的 `PRD-v0.1.md`，然后修订 `PRD-v0.1.md`。

执行边界：

1. 只修改 PRD，不编写业务代码，不部署服务，不连接或清理任何 VPS。
2. 保留原始愿景，但把“已冻结架构”改为“待能力验证的候选架构”。
3. 将本文所有 P0 项写入新版 PRD；P1 项应进入实施阶段或明确的后续范围。
4. 不得把未经官方文档、源码或可运行 spike 证明的 dsh 能力写成既成事实。
5. 每个关键能力必须给出验证方法、通过标准、失败后的替代方案。
6. 修订后将版本提升为 `v0.2`，新增“架构假设与验证状态”“安全边界”“任务状态机”“测试与验收指标”章节。
7. 修订完成后输出一份变更摘要，逐条说明本报告中的问题如何被处理。

---

## 2. 总体结论

Fish Harness 的产品方向成立：用手机作为控制入口，让远程 AI 执行环境持续完成调研、代码和内容任务，能够解决真实的跨设备、长任务和异步交付问题。

但当前 PRD 把三类尚未验证的假设同时当成了事实：

- dsh 已经具备远程多机调度、daemon worker、任务迁移和三层代理抽象；
- 运行中的 AI 任务可以在不同机器之间安全转移；
- 用 Basic Auth 暴露一个具有 shell、代码和密钥访问权的系统足够安全。

这些假设一旦有一项不成立，现有架构、工期和安全结论都会失真。因此当前版本不能保持“架构设计冻结，待 MVP 实施”的状态。

### 审验评分

| 维度 | 当前评分 | 修订目标 |
|------|----------|----------|
| 产品价值 | 7/10 | 8/10 |
| 技术可行性 | 5/10 | 8/10 |
| MVP 可交付性 | 4/10 | 8/10 |
| 安全与权限 | 3/10 | 8/10 |
| 可测试性 | 3/10 | 8/10 |
| 可运维性 | 4/10 | 8/10 |

### 推荐路线

采用“**单工作流纵向闭环 + 底层能力 spike**”：

- 先证明一个真实、高频工作流能够从手机派工到结构化结果交付；
- 在该闭环的第一阶段验证 dsh 的真实能力边界；
- 暂不建设三 commander、六 host、自动迁移和 GPU 弹性池；
- dsh 先作为可替换执行器，不作为未经验证的完整分布式控制平面。

---

## 3. P0 阻塞项

### P0-1：dsh 能力覆盖率未经验证

**置信度：9/10**

PRD 原文：

> “DeepSeek Harness（dsh），覆盖 80% 需求”
> “dsh 覆盖度 80%：调度骨架、LTM、compaction、subagent 多变体”

官方仓库将 dsh 标记为 developer preview，并明确提示会出现破坏性兼容变更。官方快速入门证明了 Web UI、工作区、命令执行、委派和审批能力，但不能直接证明 PRD 中的远程 agent 注册、`orchestrator + commander + worker` 守护进程、跨主机重派和任务恢复能力。

参考：

- https://github.com/deepseek-ai/deepseek-harness
- https://deepseek-harness.github.io/deepseek-harness/guide/quickstart

**要求 Claude Code 修改：**

- 删除“覆盖 80%”这一未经度量的结论。
- 新增 dsh 能力验证矩阵，至少包含：官方支持状态、验证命令、验证结果、适配成本和 fallback。
- 将以下 CLI 和接口全部标记为“待验证”，在 spike 通过前不得进入正式部署清单：
  - `dsh orchestrator start`
  - `dsh commander start`
  - `dsh agent start`
  - 远程 worker 注册与心跳
  - WebSocket/JSON-RPC 跨主机协议
  - 任务持久化与恢复
  - 运行中任务重派
  - 多模型路由配置格式
- 将 dsh API 变更风险从“低”提升为“高”。
- 给出 fallback：若 dsh 不提供上述能力，则使用薄控制层、持久化队列和单机执行适配器，不 fork dsh 核心。

### P0-2：持久化不能从 MVP 中移除

**置信度：10/10**

PRD 一方面要求任务运行最长 24 小时、断线后继续执行并推送结果，另一方面又把持久化推迟到 MVP 之后。这两个要求互相冲突。

**要求 Claude Code 修改：**

- 将 SQLite 任务账本提升为 MVP P0。
- MVP 至少持久化：
  - `task_id`
  - 原始指令与结构化任务参数
  - 创建时间、更新时间、截止时间
  - 当前状态与状态版本
  - 执行 worker、lease 到期时间
  - 尝试次数、最近错误码
  - 产物位置与摘要
  - 审批记录、取消原因
- 明确进程重启、服务器重启和 WebSocket 断线后如何恢复。
- 禁止只把 dsh session 当成唯一事实来源。

### P0-3：“自动转移活跃任务”当前不可实现

**置信度：9/10**

运行中的编码任务可能包含未提交文件、子进程、缓存、凭证、本地数据库和不可逆外部副作用。心跳失败后直接把同一任务派给另一台机器，可能造成重复提交、重复发布、文件覆盖或两个 worker 同时执行。

**要求 Claude Code 修改：**

- MVP 删除“运行中任务自动迁移”的承诺。
- 区分：
  - `queued` 任务可安全重新分配；
  - `running` 任务只有在明确 checkpoint 后才可恢复；
  - 产生外部副作用的任务默认不得自动重放。
- 引入 lease、幂等键、fencing token、checkpoint 和人工恢复入口。
- 第一版采用“失败后暂停并通知用户”，而不是未经证明的透明迁移。

建议状态机：

```text
                     ┌───────────┐
                     │ cancelled │
                     └─────▲─────┘
                           │ cancel
created ──▶ validated ──▶ queued ──▶ leased ──▶ running ──▶ succeeded
              │             │          │           │
              │ reject      │ timeout  │ lease     │ retryable error
              ▼             │          │ expired   ▼
           rejected         └──────────┴──────▶ retry_wait
                                                   │
                                                   ├──▶ queued
                                                   └──▶ failed

非法转换：
- succeeded/failed/cancelled 不得重新进入 running
- 没有有效 fencing token 的 worker 不得提交结果
- running 不得直接迁移到另一 worker，只能从已确认 checkpoint 恢复
```

### P0-4：公网执行入口的安全模型不足

**置信度：10/10**

该系统能够执行 shell、修改代码、调用外部 API 并读取多个项目凭证。Basic Auth 泄露后的实际后果接近远程代码执行和全环境密钥泄露，不能以“个人项目”为理由降低标准。

**要求 Claude Code 修改：**

- 首选 Tailscale/WireGuard/Cloudflare Access 等私网或设备身份入口，不直接把控制平面暴露为普通公网 Web 应用。
- 禁止仅依赖 Basic Auth；如保留，只能作为第二层防护。
- 增加短期 session、设备撤销、速率限制、CSRF 防护、WebSocket 鉴权和重放保护。
- worker 使用独立身份和最小权限，不共享全局 API Key。
- 不同项目的工作目录、密钥、网络访问和命令权限必须隔离。
- 危险操作必须显式审批：发布、删除、数据库迁移、权限修改、购买 GPU、发送外部消息。
- 写入不可篡改或追加式审计日志：谁在何时下达什么任务、哪个 worker 执行、调用了什么工具、产出了什么结果。
- 增加 prompt injection 威胁：外部网页、视频字幕和仓库内容均视为不可信输入，不能自动提升为系统指令。

### P0-5：MVP 没有验证真实产品价值

**置信度：9/10**

PRD 将用户的主要痛点定义为信息检索和视频工作流，但 MVP 选择了通用“独立任务”，并优先实现语音 UI。这样可能证明技术入口可用，却不能证明系统是否持续节省时间。

**要求 Claude Code 修改：**

- 将 MVP 收缩为一个高频、可重复、可衡量的工作流。
- 推荐首个工作流：输入频道或主题，获取最近内容，产出带来源的结构化研究简报。
- 第一阶段使用文字输入；语音、PWA 安装和 Web Push 在核心闭环稳定后加入。
- MVP 验收以真实任务运行数据为准，不以“容器全部 running”为准。

---

## 4. P1 高优先级改进

### P1-1：调度算法必须先过滤硬约束，再评分

当前评分函数会给能力不匹配的 worker 一个较低分，而不是直接排除。例如需要 Xcode 的任务只能派给具备 Xcode、正确代码和授权凭证的在线 worker。

修订为：

```text
任务规范化
   │
   ▼
硬约束过滤：在线、能力、架构、工作目录、凭证、内存、网络策略
   │ 无候选
   ├────────▶ blocked + 明确原因 + 通知用户
   ▼
软评分：locality、成本、负载、时延、可靠性、工作时段
   │
   ▼
原子 lease ──▶ 执行 ──▶ checkpoint/result
```

需要补充：并列候选、资源预留、并发上限、饥饿防止、队列优先级和 worker draining。

### P1-2：模型路由不应绑定未经验证的固定模型名

模型名称、价格、限流和能力会变化。PRD 应按能力等级描述模型选择，并把具体 provider/model ID 放入运行时配置。

建议路由维度：

- 任务风险和审批等级
- 上下文长度
- 工具调用可靠性
- 结构化输出通过率
- 延迟与预算
- provider 可用性和地区限制
- 质量 eval 分数

每种 fallback 都必须重新校验工具权限和输出格式，不能只更换模型字符串。

### P1-3：资源预算和故障域需要重算

Swap 只能缓解瞬时内存压力，不能等价于可用 RAM。newvps 同时承载控制平面、Web UI、状态存储和多个 AI CLI 子进程，会形成资源争抢和单点故障。

PRD 应新增：

- 每个服务的 RSS、CPU、磁盘、文件描述符和并发预算；
- 单 worker 一次只运行一个重型任务的默认限制；
- OOM、磁盘满、僵尸子进程和日志膨胀测试；
- 控制平面与执行进程的资源隔离；
- 数据库备份与恢复演练；
- newvps 故障时系统进入“不可接单但不丢任务”的降级模式。

### P1-4：工程量不能用代码行数做主要依据

`2500-3000 行 TypeScript` 无法反映分布式一致性、安全、部署和测试成本。完整版控制平面还需要协议、持久化、并发控制、审计、密钥隔离、恢复和运维工具，实际规模可能明显更大。

请改为按里程碑、接口和验收结果估算，并给出乐观、基准、悲观三档。

### P1-5：语音与通知属于体验层，不是 MVP 的首要风险

Web Speech API、iOS 后台行为和 Web Push 需要真机验证，但这些不应阻塞底层任务闭环。第一版必须保留文字输入、刷新恢复和站内任务列表，不能把实时 WebSocket 当成唯一状态来源。

---

## 5. 建议的 MVP 架构

```text
┌──────────────────────────────┐
│ iPhone Web UI                │
│ 文本派工、审批、状态、结果    │
└──────────────┬───────────────┘
               │ HTTPS + device identity
               ▼
┌──────────────────────────────┐
│ Thin Control API             │
│ validation / auth / audit    │
└──────────────┬───────────────┘
               │ transaction
               ▼
┌──────────────────────────────┐
│ SQLite Task Ledger           │
│ task / lease / event / result│
└──────────────┬───────────────┘
               │ poll or claim
               ▼
┌──────────────────────────────┐
│ One Worker Adapter           │
│ dsh if spike passes          │
│ fallback: cc/codex subprocess│
└──────────────┬───────────────┘
               │ isolated workspace
               ▼
┌──────────────────────────────┐
│ Research Workflow            │
│ fetch → transcribe → cite →  │
│ summarize → persist artifact │
└──────────────┬───────────────┘
               ▼
┌──────────────────────────────┐
│ Result Page + Notification   │
│ DB is truth; push is a hint  │
└──────────────────────────────┘
```

### 架构原则

1. 数据库是任务状态的唯一事实来源，WebSocket 和推送仅用于通知。
2. 调度器只发放有期限的 lease，不直接相信 worker 在线状态。
3. worker 输出必须包含 `task_id`、`attempt_id` 和 fencing token。
4. 每个任务在隔离工作目录运行，禁止任意绝对路径。
5. dsh 通过 adapter 接入，避免业务状态依赖其内部数据结构。
6. MVP 只支持一个执行 worker；多机调度在单机闭环通过后再启用。

---

## 6. 错误与恢复要求

| 路径 | 失败模式 | 系统动作 | 用户看到 |
|------|----------|----------|----------|
| 创建任务 | 空输入、过长输入、非法工作流 | 拒绝并记录校验错误 | 可修正的明确提示 |
| 模型调用 | 超时、429、空响应、拒答、畸形 JSON | 分类重试；超过预算转人工 | 当前尝试和下一步 |
| worker claim | 两个 worker 同时抢任务 | 数据库原子 lease，只允许一个成功 | 无感知 |
| worker 心跳 | worker 离线 | lease 到期；有 checkpoint 才允许重试 | “执行中断，等待恢复” |
| 任务执行 | OOM、磁盘满、子进程退出 | 终止 attempt，保存 stderr 和资源数据 | 失败原因与重试入口 |
| 结果提交 | 旧 worker 晚到提交 | fencing token 拒绝陈旧结果 | 保留有效结果 |
| 外部抓取 | 403、限流、内容删除 | 退避或标记部分成功 | 缺失来源列表 |
| 通知 | Web Push 失败 | 任务仍为成功；站内可查询 | 下次打开可见 |
| 数据库 | 锁、损坏、磁盘满 | 停止接单、告警、进入只读模式 | “系统暂不可接单” |

禁止出现“记录日志后吞掉错误并继续”的实现。每个错误必须归类为可重试、不可重试、需要审批或系统故障。

---

## 7. 安全威胁模型最低要求

| 威胁 | 可能性 | 影响 | 必须措施 |
|------|--------|------|----------|
| 控制入口凭证泄露 | 中 | 极高 | 设备身份、短期 session、撤销、限流 |
| Prompt injection 诱导执行命令 | 高 | 极高 | 不可信内容隔离、工具 allowlist、审批 |
| 路径穿越读取其他项目 | 中 | 高 | 工作区 allowlist、真实路径校验、容器隔离 |
| worker 被攻陷后横向移动 | 中 | 极高 | 每 worker 独立凭证、最小权限、网络分段 |
| WebSocket 劫持或重放 | 中 | 高 | 鉴权、origin 校验、nonce、短期 token |
| 重试造成重复发布或重复付费 | 中 | 高 | 幂等键、外部副作用审批、预算上限 |
| 日志泄露 API Key 或私有代码 | 中 | 高 | 字段级脱敏、保留期、访问控制 |
| 供应链依赖被污染 | 中 | 高 | 版本锁定、镜像扫描、SBOM、升级验证 |

---

## 8. 测试与验收标准

### 8.1 dsh 能力 spike

- [ ] 从干净环境启动官方 Web UI。
- [ ] 验证模型配置、工作区选择、命令执行和审批。
- [ ] 验证官方是否存在远程 worker/daemon API；保存版本、命令和输出证据。
- [ ] 验证进程重启后 session、任务和结果是否恢复。
- [ ] 验证 API/SDK 能否被薄 adapter 稳定调用。
- [ ] 证明升级或失败时可以切换为 cc/codex subprocess adapter。

### 8.2 单元测试

- [ ] 任务输入校验：nil、空、超长、Unicode、错误工作流类型。
- [ ] 状态机所有合法和非法转换。
- [ ] 硬约束过滤与无候选 worker。
- [ ] lease 获取、续租、到期和 fencing token。
- [ ] 重试分类、指数退避、最大预算。
- [ ] 凭证和日志脱敏。

### 8.3 集成测试

- [ ] API → task ledger → worker claim → result 的完整链路。
- [ ] 控制平面重启后任务不丢失。
- [ ] worker 在执行中退出，任务进入可解释状态。
- [ ] 两个 worker 同时 claim 时只允许一个成功。
- [ ] 旧 worker 不能覆盖新 attempt 的结果。
- [ ] WebSocket 和 Web Push 失败不影响结果查询。

### 8.4 E2E 与混沌测试

- [ ] iPhone 创建研究任务并最终看到带来源结果。
- [ ] 双击派工不会生成两个任务。
- [ ] 页面关闭、网络切换或 session 过期后可恢复查看。
- [ ] 模型 429、抓取 403、进程 OOM、磁盘接近满时行为符合错误表。
- [ ] 未授权设备、路径穿越和 prompt injection 攻击被拒绝并产生审计记录。

### 8.5 MVP 产品验收

建议以连续 20 个真实任务为样本：

- 任务状态丢失：0 次；
- 无人工介入完成率：至少 80%；
- 失败原因可解释率：100%；
- 可恢复失败比例：至少 95%；
- 危险操作审批覆盖率：100%；
- 结果包含可核验来源的比例：100%；
- 用户每周真实使用：至少 3 次，连续观察 4 周；
- 节省时间：相对人工流程中位数至少 50%。

---

## 9. 建议实施顺序

| 阶段 | 目标 | 退出条件 |
|------|------|----------|
| 0 | dsh 能力 spike | 能力矩阵有证据，确定 adapter 边界 |
| 1 | 持久化单机任务闭环 | 重启不丢任务，状态机与错误路径测试通过 |
| 2 | 单一研究工作流 | 20 个真实任务达到产品验收线 |
| 3 | 移动端体验 | 文字入口稳定，再增加语音与推送 |
| 4 | 第二 worker + lease | 并发 claim、离线、陈旧提交测试通过 |
| 5 | 多机调度 | 硬约束过滤、资源预算和审计完整 |
| 6 | 视频/GPU 工作流 | 成本、审批、素材与发布幂等策略通过 |

### 并行策略

阶段 0 完成前，其余阶段不得大规模并行。阶段 1 确定任务协议后，可并行：

- Lane A：控制 API → 任务账本 → lease；
- Lane B：研究工作流 adapter → 产物格式 → 来源校验；
- Lane C：移动端文字 UI → 状态页 → 审批页。

三条 lane 都依赖统一任务协议和错误码，协议变更必须先合并再继续。

---

## 10. 明确不进入首版的范围

- 三个常驻 commander：尚未证明单 commander 是瓶颈。
- 六 host worker pool：先验证单机闭环，再增加第二 worker 验证并发协议。
- 运行中任务透明迁移：没有 checkpoint 和幂等保证前风险过高。
- 自动创建和销毁 GPU VPS：涉及付费、凭证和供应商 API，后置。
- 自动发布视频或代码：属于高副作用操作，必须在审批系统成熟后进入。
- 语音优先：首版以文字保证可靠性，语音作为后续体验增强。
- 跨项目“全权 commander”：在项目权限隔离和审计成熟前不开放。

---

## 11. Claude Code 修改 PRD 的完成清单

- [ ] 状态改为“架构候选，待 spike 验证”。
- [ ] 版本升级为 v0.2，保留 v0.1 变更历史。
- [ ] 用“单工作流纵向闭环”替换当前 MVP。
- [ ] 将任务持久化提升为 MVP P0。
- [ ] 增加任务状态机、lease、幂等和 fencing token。
- [ ] 删除运行中任务自动迁移承诺，改为 checkpoint 后恢复。
- [ ] 增加 dsh 能力验证矩阵和 fallback adapter。
- [ ] 将 dsh 兼容性风险改为高。
- [ ] 用硬约束过滤 + 软评分替换单一评分算法。
- [ ] 重写认证与授权方案，不再以 Basic Auth 作为充分措施。
- [ ] 增加 prompt injection、路径穿越、横向移动和日志泄露风险。
- [ ] 重算资源预算，明确 swap 不是 RAM。
- [ ] 将工程量从 LOC 估算改为里程碑和三点估算。
- [ ] 增加错误与恢复表、测试矩阵和产品验收指标。
- [ ] 把语音、Push、多机、GPU 和视频发布移出首版阻塞路径。
- [ ] 确保所有架构图与修订后的范围一致。
- [ ] 在 PRD 末尾添加逐条审验整改记录。

---

## 12. 复审门槛

只有同时满足以下条件，PRD 才能恢复“架构冻结，允许实施”：

1. dsh 能力矩阵有可复现证据，所有核心能力均标记为“已验证”或有明确 fallback。
2. MVP 包含持久化任务账本、状态机、错误分类和安全边界。
3. MVP 只包含一个可量化价值的工作流，并定义真实任务验收指标。
4. 公网入口不再只依赖 Basic Auth。
5. 自动迁移、重复执行和外部副作用具有明确的一致性策略。
6. 单元、集成、E2E、混沌和安全测试均进入实施计划。
7. 资源预算、备份恢复、部署顺序和回滚步骤可以执行和验证。

在以上门槛完成之前，允许进行的唯一工程工作是隔离环境中的 dsh 能力 spike 和 PRD 修订。
