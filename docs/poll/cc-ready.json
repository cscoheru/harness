{
  "task_id": "T-GH-DEPLOY-FIX",
  "commit": "pending",
  "pushed_at": "2026-09-01T07:10:00Z",
  "branch": "main",
  "notes": "T-GH-DEPLOY-FIX done. Per user choice (裁断点 B from assessment): fix deploy.yml push-job checkout gap + re-publish via tag v1.0.0a1. 1 file changed (.github/workflows/deploy.yml, +9/-0). Root cause from gh run view 33472159405 log: 'ERROR: failed to build: failed to solve: failed to read dockerfile: open Dockerfile: no such file or directory' — push job runs on a SEPARATE runner from build, and was missing actions/checkout@v4. With `context: .`, buildx resolved cwd to empty default workspace, no Dockerfile present. Build job has its own checkout (line 60) which loads image via `load: true` into that runner's daemon, but the OCI image layer is not transferable cross-runner without an OCI registry push. Two-part fix: (1) Restore P0-fix comment about smoke-location that was accidentally clobbered on first edit + add new P1-fix comment (5 lines) naming the bug + run id 33472159405 for traceability. (2) Add `Check out source` step at top of push job steps (lines 126-127), so `context: .` resolves to the actual repo root with Dockerfile present. YAML structure verified via yaml.safe_load: push job steps now = ['Check out source', 'Log in to GHCR', 'Push tag + latest']. on.triggers = ['push' (tags v*), 'workflow_dispatch']. Permissions unchanged. Plan B = ship as v1.0.0a1 patch tag, do NOT delete immutable v1.0.0a0 (GA plan §6: tag irreversible). Next: chore(poll) commit, then `git tag -a v1.0.0a1 -m ...` + `git push --tags`, poll deploy.yml workflow run for GHCR publish to `ghcr.io/cscoheru/fish-harness:v1.0.0a1` + `:latest`. Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
}