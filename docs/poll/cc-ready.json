{
  "task_id": "T-V1.2.0B-WORKER-PASS",
  "commit": "v1.2.0b cycle scope: pending user 3 commits (chore v1.2.0b audit-scope drafting + feat worker/worker_pool/execution_driver 真实现 + M4 hygiene fix + feat server.ts handleWorkerHeartbeat 真接 + chore spec/capabilities/worker.json 校准 + chore cc-ready flip + CHANGELOG + README + 集成测试 gated)",
  "created_at": "2026-09-04T18:00:00Z",
  "branch": "main",
  "status": "🟡 v1.2.0b Worker 真实现 DRAFT (wrapper/orchestrator/worker.ts 8 函数 stub → real: capability [load spec/capabilities/worker.json per F9 model_id 校准 deepseek-v4-flash + 运行时探测] / run [ExecutionDriver.run() yield DriverEvent stream] / interrupt [SIGTERM + AbortController] / heartbeat [worker_pool.heartbeat() SQLite 持久化] / health [kernel HTTP probe + version='1.2.0b' + workers_count] / register [worker_pool.register() INSERT + return worker_id per ADR 0007 schema 简化版 per F3] / drain [UPDATE drained_at + 等 active 完成] / getTaskStatus [query SQLite by task_id] + wrapper/orchestrator/worker_pool.ts NEW ~220 行 [better-sqlite3 per-host file per D1 + WAL mode + busy_timeout=5000 per ADR 0009 + ADR 0007 schema 简化版 (无 current_attempt_id FK per F3) + 6 methods: register/dispatch/heartbeat/drain/reap_stale/claim_via_pool] + wrapper/orchestrator/execution_driver.ts NEW ~200 行 [subprocess spawn 主路径 per D2 via child_process.spawn('dsh --profile headless ...') + 复用 wrapper/dsh/dsh_client.ts:137-162 spawn pattern per F4 + HTTP fallback stub fetch(DSH_HTTP_URL + '/api/v1/tasks', POST) per D2 + yield DriverEvent stream + interrupt SIGTERM + AbortController.abort()] + wrapper/orchestrator/commander.ts:113-114 TODO(v1.2.0b) 替换为真调 worker_pool.dispatch(task_id) 拿真实 worker_id per F5 + wrapper/orchestrator/server.ts handleWorkerHeartbeat 改造 PURE STUB → real [接收 body {worker_id, capabilities_json} schema validation per F6 + 调 worker.heartbeat() + worker_pool.heartbeat() SQLite 持久化 + 返回 {worker_id, status, last_heartbeat_at}] + wrapper/orchestrator/orchestrator.ts dispatch() 加 commander.aggregateResults() 后调 worker.run() 真发 step per F5 + plan §3.3 #2 + wrapper/package.json 加 better-sqlite3@^11 dep per F1 + Dockerfile 加 RUN apk add --no-cache python3 make g++ per F2 [§3.7 NEW Dockerfile 例外声明] + spec/capabilities/worker.json model_id deepseek-chat → deepseek-v4-flash + evidence_uri 字段新增 per F9 + 4 NEW unit tests [worker REWRITE ~50 + worker_pool NEW ~30 + execution_driver NEW ~20 = 100 单测] + 2 NEW integration tests gated [worker_pool ~12 + server_heartbeat ~10] + M4 hygiene fix 合并 commit 2 per D3 [vi.restoreAllMocks → vi.clearAllMocks in afterEach]). 20 文件改动 (6 wrapper/orchestrator/ 代码 [worker REWRITE + worker_pool NEW + execution_driver NEW + commander EDIT + server EDIT + orchestrator EDIT] + 1 wrapper/package.json + 1 Dockerfile + 1 spec/capabilities/worker.json + 6 wrapper/test/ tests [setup.ts 不变 + worker.test.ts REWRITE ~50 + worker_pool.test.ts NEW ~30 + execution_driver.test.ts NEW ~20 + integration worker_pool.test.ts NEW ~12 gated + integration server_heartbeat.test.ts NEW ~10 gated + 2 integration M4 fix] + 2 notes/ v1.2.0b audit-scope/prompt + 3 docs/ cc-ready + CHANGELOG + README). 实测: tsc exit 0 + vitest baseline v1.2.0a formal PASS 实测 + v1.2.0b commit 2 增量 (≥ 40 单测 [worker REWRITE + worker_pool + execution_driver] + 22 gated 集成 [worker_pool 12 + server_heartbeat 10]) + hygiene §1-§4.11 全过 (不锁型号 tracked 引用式 v1.2.0a §1.5 主表合计 [v1.2.0a 收口 289e7eb 实测 = 引用式不复制] / disk 引用式 v1.2.0a §1.5 主表 disk 行 / 前向交付物 == 0 / wrapper/orchestrator/ == 0 / DEEPSEEK_API_KEY 字面 == 0 / VAPID 私钥字面 == 0 / §3.7 NEW Dockerfile 例外 ≥ 1 [RUN apk add python3 make g++] / TODO(M1) in worker.ts == 0 / TODO(M1) wrapper/orchestrator/ == 0 / ExecutionDriver|worker_pool refs in worker.ts ≥ 6 / better-sqlite3|Database refs in worker_pool.ts ≥ 4 / WAL|busy_timeout|journal_mode in worker_pool.ts ≥ 3 / child_process|callDshHeadless in execution_driver.ts ≥ 3 / fetch.*api/v1 in execution_driver.ts ≥ 1 / worker.heartbeat|worker_pool.heartbeat in server.ts ≥ 2 / worker.health() version='1.2.0b' ≥ 1 / spec/capabilities/worker.json deepseek-v4-flash ≥ 1 / §4.10.5 NEW TODO(v1.2.0b) in commander.ts == 0 / §4.10.6 NEW stub-worker-${taskId} in commander.ts == 0 / §2.7 NEW WORKER_POOL_DB=/data/ in worker_pool.ts == 1 / v0.7 §4.5.7 5 edge compose 守门 [sleep infinity == 0 / harness-edge[1-5] ≥ 5 / tag:harness-edge ≥ 1] 维持 / v0.7 §4.7.6 server.ts 8 endpoint 守门 [app.{get,post,use} = 8] 维持 / v1.2.0a §4.10 commander 真实现守门 [TODO(M1) in commander.ts == 0 / WorkflowPack refs = 3 / PlanPlan|PlanStep refs = 10 / AggregateError refs = 5 / orchestrator.ts 真走 commander = 7] 维持 / v0.7 §4.8 PROJECT_ROOT 路径修法 [import.meta.url ≥ 4 in 4 dsh files + 残留 == 0 + §4.8.5 NEW wrapper/orchestrator/ 5 文件 ≥ 2] 维持 / v0.7 §4.9 dsh binary install 守门维持). Pending user (7 EXEC items per plan §11.5 U1-U7): (U1) TypeScript build on newvps (ssh puer-hk + cd /opt/fish-harness/wrapper + npm install --no-save better-sqlite3@^11 + ./node_modules/.bin/tsc), (U2) 双 gate 验证 (ssh puer-hk + cd /opt/fish-harness/wrapper + ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run), (U3) npm install + Docker build + restart (per v1.2.0b 范围 — 新增 better-sqlite3 dep + Dockerfile build tools; npm install 触发 native build + docker build -t fish-harness-wrapper:latest . 视情况可省 [wrapper 走 bind mount 部署, host-side npm install 后 bind mount 进容器即可] + docker compose -f deploy/6host-compose.newvps.yml up -d --force-recreate 重启 5 wrapper 容器), (U4) 真机 E2E 套件真调 (RUN_WORKER_POOL_E2E=1 RUN_SERVER_HEARTBEAT_E2E=1 RUN_ORCH_COMMANDER_E2E=1 RUN_PACK_PLAN_E2E=1 DEEPSEEK_API_KEY=<key> ssh puer-hk 'cd /opt/fish-harness/wrapper && ./node_modules/.bin/vitest run test/integration/{worker_pool,server_heartbeat,orch_commander,pack_plan}.test.ts'), (U5) 4+1 Funnel URL 路径 200 验证 (per v1.2.0b — /api/v1/worker/health + /api/v1/worker/heartbeat + /api/v1/commander/health + /api/v1/tasks + /api/v1/status/test 5 路径 all 200), (U6) Codex v1.2.0b formal 复审 (user 亲提 Codex CLI model=gpt-5.6-sol + reasoning_effort=xhigh 复审 v1.2.0b audit-scope → 预期 0C/0M/0m + §4.11 worker 真实现守门 14 项全绿 + §4.7.7 server.ts handleWorkerHeartbeat 真接守门全绿 + §4.10.5/§4.10.6 commander.ts TODO(v1.2.0b) + synthetic stub 替换守门全绿 + §3.7 Dockerfile 例外声明合规 + §2.7 better-sqlite3 path 默认值守门全绿 + tracked 锚定 post-v1.2.0b 引用式 PASS), (U7) v1.2.0b minor tag (user 亲提 git tag -a v1.2.0b + push via Clash proxy git -c http.proxy=127.0.0.1:7890 -c https.proxy=127.0.0.1:7890 push origin v1.2.0b).",
  "files_modified": [
    "notes/codex-audit-scope-v1.2.0b-v0.1.md (NEW v1.2.0b 守门: 继承 v0.7 §1-§9 + v1.2.0a §1-§9 全套守门 + §4.11 NEW worker 真实现守门 [TODO(M1) in worker.ts == 0 + TODO(M1) wrapper/orchestrator/ == 0 + ExecutionDriver|worker_pool refs ≥ 6 + better-sqlite3|Database refs ≥ 4 + WAL|busy_timeout|journal_mode ≥ 3 + child_process|callDshHeadless ≥ 3 + fetch.*api/v1 ≥ 1 + server.ts worker.heartbeat|worker_pool.heartbeat ≥ 2 + worker.health() version='1.2.0b' ≥ 1 + spec/capabilities/worker.json deepseek-v4-flash ≥ 1 + 单测增量 ≥ 40 + 集成测试 gated ≥ 2 + worker_pool.ts + execution_driver.ts file exists + better-sqlite3 dep ≥ 1 + Dockerfile apk add ≥ 1] + §4.7.7 NEW server.ts handleWorkerHeartbeat 真接守门 + §4.10.5/§4.10.6 NEW commander.ts TODO(v1.2.0b) + synthetic stub 替换守门 + §3.7 NEW Dockerfile 例外声明 + §2.7 NEW better-sqlite3 path 默认值守门 + §4.8.5 NEW wrapper/orchestrator/ 5 文件 PROJECT_ROOT 守门 + §5 20 文件 hygiene 自检表 + §7 v1.2.0b NEW 教训记档 [worker 真实现 stub → real + better-sqlite3 per-host WAL + ExecutionDriver both 模型 + server.ts heartbeat 真接 + M4 hygiene fix 合并 commit 2 + spec 校准 + 7 user must execute] + §9 14 验证命令矩阵)",
    "notes/codex-audit-scope-v1.2.0b-v0.1-prompt.md (NEW v1.2.0b Codex 复审 prompt + 14 hygiene checklist + 6 处引用式机制落地验证 + 25 条验证命令 + §6 v1.2.0b §8 复审环境注记 [better-sqlite3 native build + Dockerfile 例外 + wrapper bind mount 部署 + M4 hygiene fix 合并 commit 2])",
    "wrapper/orchestrator/worker.ts (REWRITE ~280 行: 8 函数 stub → real — capability() 读 spec/capabilities/worker.json [per F9 model_id 校准 deepseek-v4-flash + evidence_uri] + 运行时探测 [CPU cores + memory] + max_concurrent_attempts 1 + supports_streaming/interrupt/heartbeat 3 true + supports_tool_gateway false; run() 调 ExecutionDriver.run() yield DriverEvent stream; interrupt() 发 SIGTERM + 写 worker_pool cancelled 状态; heartbeat() 调 worker_pool.heartbeat() + 写 SQLite; health() kernel HTTP probe + 返回 {version:'1.2.0b', workers_count, status: 'ok' | 'error'}; register() INSERT worker + 返回 worker_id per ADR 0007 schema 简化版 per F3 [无 current_attempt_id FK]; drain() UPDATE drained_at + 等 active 完成; getTaskStatus() query SQLite by task_id)",
    "wrapper/orchestrator/worker_pool.ts (NEW ~220 行, per ADR 0007 schema 简化版 + D1 better-sqlite3 per-host file per ADR 0009 single-host WAL constraint: import Database from 'better-sqlite3' + new Database(/data/worker_pool.db) per §2.7 NEW WORKER_POOL_DB env override + db.pragma('journal_mode = WAL') + db.pragma('busy_timeout = 5000') + CREATE TABLE workers (worker_id TEXT PRIMARY KEY, host TEXT, capabilities_json TEXT, status TEXT, last_heartbeat_at INTEGER, registered_at INTEGER, drained_at INTEGER) [简化 schema per F3 无 current_attempt_id FK wrapper 是 pure client 不持有 cross-DB FK kernel-side authoritative] + 6 methods: register(host, capabilities_json) → INSERT + return worker_id / dispatch(task_id, capability) → 选 worker by capability matching + return DispatchResult {worker_id, strategy} / heartbeat(worker_id) → UPDATE last_heartbeat_at + return {worker_id, status, last_heartbeat_at} / drain(worker_id) → UPDATE drained_at + 等 active 完成 / reap_stale(max_idle_ms) → DELETE WHERE last_heartbeat_at < ? / claim_via_pool(task_id) → SELECT active worker + return DispatchResult; 2 invariants [heartbeat advance last_heartbeat_at + drain 设 drained_at])",
    "wrapper/orchestrator/execution_driver.ts (NEW ~200 行, per D2 both 模型: 主路径 subprocess spawn via child_process.spawn('dsh --profile headless ...') 复用 wrapper/dsh/dsh_client.ts:137-162 spawn pattern per F4 [import { callDshHeadless } from '../dsh/dsh_client.js'] + 备用路径 fetch(DSH_HTTP_URL + '/api/v1/tasks', POST) HTTP fallback stub per D2 [当前 dsh 不暴露 HTTP, stub 预留接口等 v1.2.0c 真接 MagicDNS 远程 host]; yield DriverEvent stream [driver.started → output_chunk ×N → heartbeat ×N → finished] + interrupt() 发 SIGTERM + AbortController.abort() + dsh_client.ts runWithTimeout AbortSignal.timeout 兜底)",
    "wrapper/orchestrator/commander.ts (Edit: commander.ts:113-114 TODO(v1.2.0b) 替换为真调 worker_pool.dispatch(task_id) 拿真实 worker_id 写 PlanStep.worker_id per F5; dispatchStep 不再用 synthetic stub-worker-${taskId}-${stepName} stub [§4.10.6 NEW 守门]; §4.10.5 NEW TODO(v1.2.0b) marker == 0)",
    "wrapper/orchestrator/server.ts (Edit: handleWorkerHeartbeat PURE STUB {status:'ok', heartbeat:true} 改造为真接 worker.heartbeat() + worker_pool.heartbeat() SQLite 持久化 per F6; 接收 body {worker_id, capabilities_json} schema validation [worker_id 非空 + capabilities_json ≤10KB + reject extra fields 注入防护]; 返回 {worker_id, status, last_heartbeat_at}; §4.7.7 NEW server.ts worker.heartbeat|worker_pool.heartbeat refs ≥ 2 守门)",
    "wrapper/orchestrator/orchestrator.ts (Edit: dispatch() 在 commander.aggregateResults() 后调 worker.run() 真发 step per F5 + plan §3.3 #2; 保留 backward-compat kernel + dsh 调用; §4.10 维持 orchestrator.ts 真走 commander ≥ 3)",
    "wrapper/package.json (Edit: 加 'better-sqlite3': '^11' dep per F1 + newvps npm install 触发 fetch + native build)",
    "Dockerfile (Edit: 加 'RUN apk add --no-cache python3 make g++' per F2 — node:22-alpine 默认无 python3/make/g++, better-sqlite3 native 编译必备; §3.7 NEW Dockerfile 例外声明 — Dockerfile 修改不破 v1.0 runtime 0 行 diff 守门 [§3 第一条 diff 范围排除 Dockerfile])",
    "wrapper/test/unit/worker.test.ts (REWRITE ~50 tests: 30+ stub shape → real shape per F8 — capability from spec/capabilities/worker.json [model_id deepseek-v4-flash] + heartbeat from worker_pool + run via ExecutionDriver DriverEvent stream + interrupt SIGTERM + register worker_pool INSERT + drain UPDATE + getTaskStatus query SQLite)",
    "wrapper/test/unit/worker_pool.test.ts (NEW ~30 tests: SQLite 持久化 + 6 methods + WAL mode + busy_timeout + reap_stale 过期 worker 清理 + claim_via_pool capability matching)",
    "wrapper/test/unit/execution_driver.test.ts (NEW ~20 tests: DriverEvent stream 流式输出 + interrupt AbortController + spawn timeout fallback + HTTP fallback stub per D2)",
    "wrapper/test/integration/worker_pool.test.ts (NEW ~12 tests: register → heartbeat → dispatch → drain flow; gated by RUN_WORKER_POOL_E2E=1 per F10)",
    "wrapper/test/integration/server_heartbeat.test.ts (NEW ~10 tests: HTTP POST /api/v1/worker/heartbeat → worker.heartbeat() → SQLite 持久化; gated by RUN_SERVER_HEARTBEAT_E2E=1; 替换 server_integration.test.ts:49-55 stub assertion per F7)",
    "wrapper/test/integration/orch_commander.test.ts (Edit M4 fix per D3: vi.restoreAllMocks() → vi.clearAllMocks() in afterEach per F10 — vi.restoreAllMocks 破坏 vi.mock factory 而 vi.clearAllMocks 仅清状态保 factory)",
    "wrapper/test/integration/pack_plan.test.ts (Edit M4 fix per D3: vi.restoreAllMocks() → vi.clearAllMocks() in afterEach per F10 — 同上)",
    "spec/capabilities/worker.json (Edit: model_id 'deepseek-chat' → 'deepseek-v4-flash' per F9 与 docs/m0b/profile-override-worker.yaml M2 校准对齐 + evidence_uri 'spec/capabilities/worker.json' 字段新增 per ADR 0007 capability evidence 要求)",
    "docs/poll/cc-ready.json (task_id T-V1.2.0A-COMMANDER-PASS → T-V1.2.0B-WORKER-PASS + status 翻牌 + 20 文件清单 + D1/D2/D3 决策记档 + M4 hygiene fix 归入 + 7 user EXEC 挂账)",
    "CHANGELOG.md ([1.2.0b] minor 段 NEW: worker 真实现 PASS marker + worker_pool 真实现 + execution_driver 真实现 + server.ts handleWorkerHeartbeat 真接 + M4 hygiene fix 归 v1.2.0b + D1=B better-sqlite3 per-host + D2=C subprocess+HTTP fallback + D3=A M4 合并 commit 2 + better-sqlite3@^11 dep + Dockerfile build tools + spec/capabilities/worker.json 校准 + 3 NEW files + 4 NEW unit tests + 2 NEW integration tests gated + 3 commits 实施 + user 7 EXEC 挂账)",
    "README.md (v1.2.0b status 段补: better-sqlite3 per-host + ExecutionDriver both + M4 hygiene + 7 user EXEC + v1.2.0c 后续 sub-cycle 预告)"
  ],
  "files_modified_count": 20
}
