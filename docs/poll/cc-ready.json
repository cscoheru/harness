{
  "task_id": "T-QA-3",
  "commit": "370bdce",
  "pushed_at": "2026-08-31T14:09:00Z",
  "branch": "main",
  "notes": "T-QA-3 done. Benchmark suite shipped: harness/benchmark/__init__.py (PEP 562 lazy import to avoid Python 3.14 RuntimeWarning on python -m invocation) + harness/benchmark/runner.py (CLI argparse --tasks/--workers/--out/--csv; percentile helper; writes results.json + summary CSV; hard gate p99 < 5000ms exit 0 else 1). Core loop uses make_db() file DB (NOT :memory: — must exercise WAL page cache) + SqliteWorkerPool.register N + seed_task M; per task, time dispatch_worker + claim (NOT claim_via_pool — its hard-coded _now_iso(offset_seconds=15.0) triggers I16 backslide on iter>=2), then transition attempt → succeeded + workers.current_attempt_id = NULL so partial UNIQUE idx_worker_one_active_attempt allows re-dispatch. Deliberately skip last_heartbeat_at bump (sub-ms I16 ties + dispatch_worker doesn't read heartbeat). Smoke --tasks=10 --workers=2: p99=0.43ms / 3695 tasks/s; default --tasks=50 --workers=4: p99=1.49ms / 4128 tasks/s, exit 0. No regression: pytest tests/ -q 37/37 PASS + python3 -m harness.testing.mutation_suite 17/17 PASS. NEXT GUN: T-QA-4 (3 new CI jobs: integration-tests / mutation-suite / benchmark-baseline per GA plan §2) or T-QA-5 (50×200 stress test) as fallback."
}