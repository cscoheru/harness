{
  "task_id": "T-QA-1",
  "commit": "2a223b2",
  "pushed_at": "2026-08-31T13:50:38Z",
  "branch": "main",
  "notes": "T-QA-1 done. Phase 0 (deploy P0 fix): smoke job merged into build job — image lives in build runner daemon via load:true, separate smoke runner fails; YAML valid (jobs=build+push, no standalone smoke). Phase A: harness/testing/mutation_suite.py (1029 lines) lifts spikes/m0/mutation-test.py v0.9.4 → production CLI. 17/17 reverse-DROP causal mutations (M1-M11 + M13-M18; M12 removed). Imports prefer harness.runtime._db/workers/context; seed_blob inlined (not yet lifted, 1-stmt INSERT); M15 monkey-patch now targets harness.runtime.workers.dispatch_worker (production path, not spike _helpers). harness/testing/__init__.py uses __getattr__ lazy import (Python 3.14 RuntimeWarning fix). deploy.yml smoke step now calls 'python -m harness.testing.mutation_suite' (mutation_suite is the gate, no more 5-spike interim). Acceptance: mutation_suite.py exists (1029 lines) + host CLI exit 0 17/17 + container CLI exit 0 17/17 (no spikes bind-mount needed; production path used) + deploy.yml YAML valid + no standalone smoke job."
}