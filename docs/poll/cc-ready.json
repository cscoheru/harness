{
  "task_id": "T-QA-4",
  "commit": "d90956e",
  "pushed_at": "2026-09-01T02:35:10Z",
  "branch": "main",
  "notes": "T-QA-4 done. 3 new CI jobs shipped in .github/workflows/ci.yml: (1) integration-tests — py3.12+3.13 matrix, pytest tests/ -v (T-QA-2 37 cases, exit 0); (2) mutation-suite — py3.12+3.13 matrix, python -m harness.testing.mutation_suite (17/17 PASS, exit 0); (3) benchmark-baseline — py3.12 single runner, if: github.event_name == 'workflow_dispatch' ONLY (GA plan §5 R-5 CI resource guard), runs runner --tasks=50 --workers=4, Python reads results.json['passes_gate'] and exit 1 on fail, then actions/upload-artifact@v4 (path: results.json, retention 14d, if-no-files-found: error). 3 jobs independent (no needs); per-job concurrency group ci-{workflow}-{ref} cancel-in-progress=false; per-job permissions: contents: read (least privilege — packages: write is deploy.yml's job); pip cache via actions/setup-python@v5 cache='pip' keyed on pyproject.toml. YAML valid (python3 -c 'import yaml; safe_load(...)'). Host simulation: pytest 37/37 PASS in 0.34s + mutation 17/17 PASS + benchmark passes_gate=true. Out of scope (NOT touched): .github/workflows/deploy.yml (T-QA-1 P0 fix preserved) + m0-contract-tests.yml (12 spike jobs) + spikes/m0/_helpers.py + spec/kernel-schema.sql. Architect self-signed REVIEW-T-QA-4.md (Cursor unavailable per GA plan §2 T-QA-4 fallback); Cursor can append signature later. NEXT GUN: T-QA-5 (50 workers x 200 tasks SQLite WAL concurrent stress test per GA plan §2) as fallback if Cursor still unavailable; else T-DD-* (docs/CHANGELOG/LICENSE/ADR 0008+0009) per GA plan §2 M3 ladder."
}