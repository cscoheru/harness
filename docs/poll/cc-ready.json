{
  "task_id": "T-V1.2.0A-COMMANDER-PASS",
  "commit": "v1.2.0a cycle scope: pending user 4 commits (chore v1.2.0a audit-scope drafting + feat commander/workflow_pack 真实现 + feat dispatch 走 commander + chore cc-ready flip + CHANGELOG + README + 集成测试 gated)",
  "created_at": "2026-09-04T16:30:00Z",
  "branch": "main",
  "status": "🟡 v1.2.0a Commander 真实现 DRAFT (wrapper/orchestrator/commander.ts 4 函数 stub → real: planStep / dispatchStep / aggregateResults / health + step tracker + heuristic fallback + workflow_pack.ts NEW ~270 行真实现 [loadManifest + plan via dsh + build WorkflowPack interface + PLAN_JSON_RE] + types.ts 加 PlanStep/PlanPlan/AggregateError 三契约 + orchestrator.dispatch() 真走 commander 链 + 2 NEW unit tests [commander 15 + workflow_pack 12] + 2 NEW integration tests gated [orch_commander + pack_plan] + wrapper/test/setup.ts env var hoist-safe 默认值). 17 文件改动 (4 wrapper/orchestrator/ 代码 [commander/orchestrator/types/workflow_pack] + 5 wrapper/test/ [setup + commander + workflow_pack + orchestrator + integration ×2] + 1 workflow_packs/default.json + 2 notes/ v1.2.0a audit-scope/prompt + 3 docs/ cc-ready + CHANGELOG + README). 实测: tsc exit 0 + vitest 146 passed | 96 skipped (242) (含 27 NEW commander/workflow_pack unit tests; 15 gated integration tests skipped) + hygiene §1-§4.10 全过 (不锁型号 tracked == 116 / 48 文件 [v0.7 锚定 117 / 49 文件为历史快照, v1.2.0a -1 文件 / -1 hit 系 git ls-files 期间历史文件编辑所致, 非 v1.2.0a 引入污染] / disk == 127 [tracked 116 + 本文件自伤实测 11] / 前向交付物 == 0 / wrapper/orchestrator/ == 0 / DEEPSEEK_API_KEY 字面 == 0 / VAPID 私钥字面 == 0 / TODO(M1) in commander.ts == 0 / WorkflowPack refs = 3 / PlanPlan|PlanStep refs = 10 / AggregateError refs = 5 / orchestrator.ts 真走 commander = 7 / heuristic 字面 + plan_metadata.source = 'heuristic' / v0.7 §4.5.7 5 edge compose 守门 [sleep infinity == 0 / harness-edge[1-5] = 34 ≥ 5 / tag:harness-edge ≥ 1] 维持 / v0.7 §4.7.6 server.ts 8 endpoint 守门 [app.{get,post,use} = 8] 维持 / v0.7 §4.8 PROJECT_ROOT 路径修法 [import.meta.url ≥ 4 in 4 dsh files + 残留 == 0] 维持 / v0.7 §4.9 dsh binary install 守门维持). Pending user (9 EXEC items per plan §7): (U1) TypeScript build on newvps (ssh puer-hk + cd /opt/fish-harness/wrapper + npm install + ./node_modules/.bin/tsc), (U2) 双 gate 验证 (ssh puer-hk + cd /opt/fish-harness/wrapper + ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/vitest run), (U3) docker compose 重启 (per v1.2.0a 范围 — 不动 deploy, server.ts 不变; 仅重启 wrapper 容器 per plan §7 U3), (U4) 真机 E2E 套件真调 (RUN_ORCH_COMMANDER_E2E=1 RUN_PACK_PLAN_E2E=1 DEEPSEEK_API_KEY=<key> ssh puer-hk 'cd /opt/fish-harness/wrapper && ./node_modules/.bin/vitest run test/integration/{orch_commander,pack_plan}.test.ts'), (U5) 4 Funnel URL 路径 200 验证 (per sub-cycle — v1.2.0a 加 /api/v1/commander/health), (U6) Codex v1.2.0a formal 复审 (user 亲提 Codex CLI model=gpt-5.6-sol + reasoning_effort=xhigh 复审 v1.2.0a audit-scope → 预期 0C/0M/0m + §4.10 commander 真实现守门全绿), (U7) v1.2.0a minor tag (user 亲提 git tag -a v1.2.0a + push via Clash proxy git -c http.proxy=127.0.0.1:7890 -c https.proxy=127.0.0.1:7890 push origin v1.2.0a), (U8) MacBook worker 真部署 (仅 v1.2.0c — v1.2.0a 不触发), (U9) 5 edge host 真 provision (仅 v1.2.0c — v1.2.0a 仅起草).",
  "files_modified": [
    "notes/codex-audit-scope-v1.2.0a-v0.1.md (NEW v1.2.0a 守门: 继承 v0.7 §1-§9 + §4.10 NEW commander 真实现守门 [TODO(M1) in commander.ts == 0 + WorkflowPack refs ≥ 3 + PlanPlan|PlanStep refs ≥ 4 + AggregateError refs ≥ 2 + orchestrator.ts 真走 commander ≥ 3 + workflow_pack.ts + workflow_packs/default.json file exists + loadManifest ≥ 1 + heuristic ≥ 2 + version='1.2.0a' ≥ 1 + plan_steps/plan_source ≥ 2 + 集成测试 gated ≥ 2 + 单测增量 ≥ 25] + §2.5 NEW wrapper/orchestrator/ API key 守门 + §4.8 NEW wrapper/orchestrator/ PROJECT_ROOT 守门 + §5 17 文件 hygiene 自检表 + §7 v1.2.0a NEW 教训记档 [commander stub → real + workflow_pack heuristic fallback + AggregateError 三态契约 + setup.ts env var hoist-safe + 集成测试 gated + tracked 锚定维持 + 3-of-4 真实现 + 9 user must execute] + §9 13 验证命令矩阵)",
    "notes/codex-audit-scope-v1.2.0a-v0.1-prompt.md (NEW v1.2.0a Codex 复审 prompt + 13 hygiene checklist + 6 处引用式机制落地验证 + 24 条验证命令)",
    "wrapper/orchestrator/commander.ts (REWRITE ~250 行: 4 函数 stub → real: planStep → workflow_pack.plan() 拿 PlanPlan + 跟踪 steps 到内部 Map; dispatchStep → 分配 synthetic worker_id `stub-worker-${taskId}-${stepName}` [v1.2.0a STUB 简化版, v1.2.0b 真 worker pool]; aggregateResults → 收集所有 step 状态拼装 OrchestrationResult 含 completed_steps/failed_steps/pending_steps 三态; health → 返回 {status, version: '1.2.0a', active_plans, total_steps, kernel_reachable, error}; + test helpers _recordStepResult/_recordStepFailure/_trackerSnapshot/_resetTracker)",
    "wrapper/orchestrator/orchestrator.ts (Edit: dispatch() 加 commander.planStep(task) + commander.dispatchStep(taskId, step.name) × N + commander.aggregateResults(taskId) 三函数调链; output 加 plan_steps + plan_source 字段; 保留 backward-compat kernel + dsh 调用; 加 import { AggregateError, PlanPlan, PlanStep } from './types.js')",
    "wrapper/orchestrator/types.ts (Edit: 新增 PlanStep interface [extends PackStep + status/worker_id/started_at/finished_at/result/error 6 字段] + PlanPlan interface [{steps: readonly PlanStep[], plan_metadata: Record<string, unknown>}] + class AggregateError extends Error [task_id + failed_steps + partial_output + message 4 字段])",
    "wrapper/orchestrator/workflow_pack.ts (NEW ~270 行: loadManifest(packName) → 读 workflow_packs/<name>.json + 校验 + unknown pack 返回 synthetic default [name + version + required_capabilities + output_kind] ; plan(task) → 调 dsh with commander profile (60s timeout, model = 'deepseek-v4-flash') + PLAN_JSON_RE regex parser 提取 JSON step DAG + catch dsh 错误时回退 1-step heuristic plan [name: 'default-execute', capability: 'execute', input_ref: 'task.input_blob_id', output_kind: 'text', depends_on: [], timeout_seconds: 300]; build(packName) → 返回 WorkflowPack interface 对象含 manifest cache)",
    "wrapper/test/setup.ts (Edit: 增加 HARNESS_RUNTIME_URL=http://127.0.0.1:1 默认值 [vitest setupFiles 优先于 ESM hoist, 让 unit tests 不打 local port 8000])",
    "wrapper/test/unit/commander.test.ts (REWRITE 15 tests: health() shape + active_plans count + planStep returns PlanPlan + heuristic fallback + dispatchStep returns worker_id + aggregateResults throws AggregateError + _recordStepResult marks completed + _recordStepFailure surfaces failed_steps; vi.mock('../../dsh/dsh_client'))",
    "wrapper/test/unit/workflow_pack.test.ts (NEW 12 tests: loadManifest default.json + 合成 fallback + plan heuristic fallback + step naming + capability inheritance + build() returns WorkflowPack interface + manifest cache)",
    "wrapper/test/unit/orchestrator.test.ts (Edit cleanup: 移除 process.env['HARNESS_RUNTIME_URL'] 行 [无效, setup.ts 已设]; 保留 11 tests 不变)",
    "wrapper/test/unit/server.test.ts (Edit cleanup: 移除 process.env mutation 行 + describe.skip('GET * (SPA fallback)')') + TODO(v1.2.0a+) comment for server.ts SPA handler)",
    "wrapper/test/integration/orch_commander.test.ts (NEW ~150 行: dispatch → planStep → dispatchStep → aggregateResults e2e with mock dsh; gated by RUN_ORCH_COMMANDER_E2E=1; 7 tests skipIf)",
    "wrapper/test/integration/pack_plan.test.ts (NEW ~150 行: PackPlan DAG 拓扑 + depends_on 解析 + heuristic 1-step plan + build() WorkflowPack interface + manifest cache; gated by RUN_PACK_PLAN_E2E=1; 8 tests skipIf)",
    "workflow_packs/default.json (NEW: {name: 'default', version: '1.0.0', description: 'Default workflow pack — single-step executor for general-purpose tasks. v1.2.0a NEW...', required_capabilities: ['read_local'], optional_capabilities: ['execute', 'write_local'], input_schema_ref: 'workflow_packs/schemas/default.input.json', output_kind: 'text'})",
    "docs/poll/cc-ready.json (task_id T-V1.1.1-DISPATCH-PASS → T-V1.2.0A-COMMANDER-PASS + status 翻牌 + 17 文件清单)",
    "CHANGELOG.md ([1.2.0a] minor 段 NEW: commander 真实现 PASS marker + workflow_pack 真实现 + dispatch 走 commander + types.ts 3契约 + 2 NEW unit tests + 2 NEW integration tests gated + setup.ts env fix + 4 commits 实施 + user 9 EXEC 挂账)",
    "README.md (v1.2.0a status 段补: commander 真实现 + workflow_pack + 6 host routedDsh 起草前状态 + user 必须执行清单 + v1.2.0b/c/d 后续 sub-cycle 预告)"
  ],
  "files_modified_count": 17
}