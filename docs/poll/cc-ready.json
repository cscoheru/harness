{
  "task_id": "T-QA-5",
  "commit": "4aba101",
  "pushed_at": "2026-09-01T02:47:31Z",
  "branch": "main",
  "notes": "T-QA-5 done. SQLite WAL concurrent stress test shipped in harness/testing/stress_test.py (374 lines). CLI: python -m harness.testing.stress_test --workers=N --tasks=N --out=path --csv=path (defaults 50/200/results.json). Design: tempfile file DB + connect_with_fk(apply_schema=True) + PRAGMA journal_mode = WAL reaffirmed per-thread; pre-seed tasks*workers task rows (default 10000) + register workers (default 50); threading.Thread x N + threading.Barrier(N) peak-concurrency start; per-iteration SINGLE TRANSACTION (BEGIN IMMEDIATE -> UPDATE tasks pending->claimed + INSERT task_attempts 'claimed' + UPDATE task_attempts claimed->succeeded + UPDATE tasks claimed->succeeded + UPDATE workers current_attempt_id=NULL -> COMMIT). Critical fix: naive _db.claim() + auto-commit UPDATE succeeded had microsecond race window (Thread B dispatch same worker between Thread A's claim commit and Thread A's UPDATE release -> I15 partial UNIQUE violation ~1-2%); collapsed to single transaction closes window. Single-transaction also requires 'claimed' intermediate status (direct INSERT 'succeeded' triggers trg_attempt_terminal_task_insert: 'cannot insert attempt for terminal task'). Host simulation: smoke 5x10 = 50/50 PASS wall=0.08s throughput=633/s; full 50x200 = 10000/10000 PASS wall=5.08s throughput=1968/s (R-2 gate 100/s x 20 headroom), p99=260ms, verification.all_match=True, all counters=0, exit 0. No regression: pytest tests/ -q 37/37 PASS + python -m harness.testing.mutation_suite 17/17 PASS + python -m harness.benchmark.runner --tasks=50 --workers=4 exit 0. Stress test NOT added to ci.yml per GA plan §5 R-5 (manual/diagnostic tool; CI minute burn risk). Architect self-signed REVIEW-T-QA-5.md (Cursor unavailable); Cursor can append signature later. NEXT GUN: T-DD-1 (README.md per GA plan §2 M3 ladder, single-file low-risk) as fallback if Cursor still unavailable; else Cursor review backfill for T-QA-3 / T-QA-4 / T-QA-5."
}