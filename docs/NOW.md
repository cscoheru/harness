# NOW — v1.0 runtime 温卡

> 改阶段只改本页。超过 200 行 = 在复制合同，改回指针。  
> 冷文档禁止整本常驻。

## 1. 阶段 / 退出

- **阶段**：v1.0 M1 Foundation（包化）。Start 门：**已开**。T-BE-5 done。
- **本阶段退出**：`pip install -e .` 成功；`import harness` 可用；原 spike suite 全绿。全里程碑见 `docs/v1.0-ga-team-plan.md` §1 / §6。
- **v1.0 完成 ≠ 手机派工**。完成 = GA plan §4 十二条 + §6 M3 tag。

## 2. 进行中

| ID | 状态 | 完成条件 |
|----|------|----------|
| T-BE-5 | ✅ done 2026-08-31 | `pyproject.toml`（name=harness, version=1.0.0a0, python>=3.12, deps=httpx>=0.28,<0.29 / jsonschema>=4.0,<5, packages find = harness*）；`harness/__init__.py` 暴露 5 Protocol（WorkerPool/EventSink/ContextManager/ArtifactStore/ToolInvocationGateway）+ ContextDistiller/ContextBudget；ContextManager = ContextDistiller & ContextBudget Protocol 组合；`pip install -e .` OK；`python3 -c "import harness; print(harness.__version__)"` → `1.0.0a0`（环境用 `python3`，NOW.md §4 命令文本不变）|
| **T-BE-1** | ✅ done 2026-08-31 | `spikes/m0/_helpers.py` (530 行) lift → `harness/runtime/{__init__,_db,workers,context}.py`；`_db.py` 暴露 `make_db/connect_with_fk/seed_task/claim/ClaimRejected`（含 schema 路径解析：`HARNESS_SCHEMA_PATH` env → `../../spec/kernel-schema.sql`）；`workers.py` 暴露 `register_worker/heartbeat_worker/drain_worker/reap_stale_workers/dispatch_worker/claim_via_pool`；`context.py` 暴露 `insert_snapshot/working_set_total/VALID_TRUST_LABELS×4`；`_helpers.py` 完整保留未删；runtime smoke + spike suite 全绿（worker-dispatch 21 cases + context-budget 全绿）|
| **T-BE-2** | ✅ done 2026-08-31 | `harness/runtime/worker_pool.py` — `SqliteWorkerPool` 实现 `WorkerPool` Protocol（6 方法：register/dispatch/heartbeat/drain/reap_stale/claim_via_pool）；内部单调时钟（offset 自 anchor 2026-08-30T12:00:00Z，heartbeat +5s, dispatch +1s）保证 I16 forward-only；`LookupError → NoWorkerAvailable` 转译；smoke 全过 + conformance 10/10 Protocols 全绿 + worker-dispatch 21 cases 无回归 |
| **T-BE-3** | ✅ done 2026-08-31 | `harness/runtime/event_sink.py` — `SqliteEventSink` 实现 `EventSink` Protocol（2 方法：`kind() -> SinkKind`、`async emit(envelope) -> SinkResult`）；写入侧由 SQLite trigger `trg_*_event_emit` 自动发 4 类 worker.* 事件到 `task_events`；sink 提供 ack 入口（per-sink 单调 seq），audit 永不失败；v1.0 仅支持 `SinkKind.AUDIT`；smoke 全过 + worker-events-emit-test 6 cases 全绿 + conformance 10/10 Protocols 全绿 |
| **T-TG-1** | ✅ done 2026-08-31 | `harness/gateway/{__init__,egress}.py` — `HttpEgressService`（pinned DNS / allowlist / 私网阻断 / redirect re-pin / 指数退避 base 0.5s factor 2 cap 8s / proxy-must-be-configured SSRF 拒绝）；`PinnedResolver` lift 自 `spikes/m0/egress-httpx-actual.py`（12 BLOCKED_NETWORKS + resolve/validate_ip/reject_rebinding 行为零变化）；`PermissionError → RedirectBlocked` 统一包装；spike 8 cases 全绿（行为零变化）+ HttpEgressService smoke（pinned / backoff / SSRF refusal / allowlist violation / redirect re-pin blocked / redirect success / ConnectError ×2 → success）全过 |
| **T-BE-4** | ✅ done 2026-08-31 | `harness/runtime/context_manager.py` — `SqliteContextManager` 实现 `ContextDistiller` + `ContextBudget` Protocol 双接口（6 方法：`distill/charge/snapshot_for_handoff/restore_handoff/remaining/total`）；写入侧走 I11（budget）trigger / I14（handoff trust）trigger；`distill` 复用 `blobs.byte_size` 列存 token_count 让 `charge` 不二次除 4；`snapshot_for_handoff` L3 parent = 最近 L2（I15 lineage）非 L1 anchor；lineage 通过 `_ensure_l1_anchor` lazy build L0+L1；compressed_token_count = `budget - ws_total`（spike L179-188 pattern）；`BudgetExceeded` / `HandoffTrustViolation` / `ContextError` 触发器异常→Protocol 异常；`python3 spikes/m0/context-budget-test.py` + `python3 spikes/m0/lineage-level-test.py` 全绿；`python3 spikes/m0/conformance-second-impl.py` 10/10 Protocols 全绿；dedicated smoke（Phase A charge 80 / Phase B handoff=20 / Phase C I11 拒绝 / Phase D remaining / Phase E restore 新 task / Phase F restore 超 budget 拒绝 / Phase G 缺失 blob）全过 |
| **T-TG-2** | ✅ done 2026-08-31 | `harness/gateway/gateway.py` — `ToolInvocationGatewayImpl` 实现 ADR 0005 6 步链（lease/fence → PDP → audit → provider → artifact_store → task_links）；写入侧 5 表持久化（`audit_log` 永写含 deny / `policy_decisions` / `approvals` 仅 needs_approval / `artifacts` 仅 allow / `task_links` 仅 allow）；lease/fence 校验走 `task_attempts` DB 读（I1 fence_version 一致性）；deny 不调 provider 且不写 artifact/link；needs_approval 写 `approvals(status='pending')` 返回 `approval_id` 不调 provider；provider-level denial 透传不写 artifact；artifact_store.put 与 artifacts.blob_id FK 解耦（gateway 分配新 blob_id）；`harness/gateway/__init__.py` export `ToolInvocationGatewayImpl` + `GatewayError`（保留 T-TG-1 的 6 export）；`python3 spikes/m0/conformance-second-impl.py` 10/10 Protocols + gateway 6 步链 全绿无回归；dedicated smoke 7 phases 全过（allow / deny / needs_approval / bad lease / bad fence / provider deny 透传 / Protocol isinstance） |
| **T-TG-3** | ✅ done 2026-08-31 | `harness/gateway/artifact_store.py` — `RealArtifactStore` 实现 `ArtifactStore` Protocol（4 方法：put/get/stat/delete，local_fs 后端）；`put` 算 sha256 → atomic temp+fsync+rename → UPSERT `blobs` 行（REAL sha256，非假值）；幂等（同 id + 同 bytes no-op）；conflict（同 id + 不同 bytes）→ `BlobConflictError`；`expected_sha256` 不匹配 → `Sha256MismatchError`（rename 前拒）；`get` 读文件重 hash → 不匹配 raise；`delete` 删文件 + blobs 行（`artifacts` FK ON DELETE RESTRICT → IntegrityError 由调用方处理）；`__init__.py` export `RealArtifactStore` + 3 异常类（保留前序 8 export）；同步重构 `gateway.py:_store_artifact` 用 PutResult 真值（删除假 sha256 预插）；`python3 -m harness.gateway.artifact_store` minimal smoke OK；8-phase dedicated smoke（put/幂等/conflict/get+篡改/stat/delete/Protocol isinstance/gateway allow 联调）全过；`pip install -e .` OK → `harness.__version__`=1.0.0a0；conformance 10/10 + 4 其他 spike suite 无回归 |
| **T-TG-4** | ✅ done 2026-08-31 | `harness/drivers/{_stub,codex_sdk,codex_exec,__init__}.py` + 2 evidence 文件 — `CodexSdkDriver` (`DriverKind.CODEX_SDK`) + `CodexExecDriver` (`DriverKind.CODEX_EXEC`) 共享 `StubDriverBase`（capability/run/interrupt/heartbeat 4 法）；`run()` 幂等缓存 `(attempt_id, fence_version) → [STARTED, FINISHED]`；`interrupt()` / `heartbeat()` 安全 no-op（FINISHED 后 interrupt 也不抛）；`capability()` 报 `supports_tool_gateway=False`（Q112：无证据 driver MUST NOT claim True）+ `max_concurrent_attempts=1` + `supports_{streaming=False,interrupt=True,heartbeat=True}`；`evidence_uri = file://harness/drivers/evidence-{sdk,exec}-stub.json`（两文件实存，3B 空 JSON）；10-phase dedicated smoke 全过（isinstance + capability 字段 + run→2 events + interrupt/heartbeat no-raise + duplicate run cached 同 identity + namespace 单类 + evidence 文件存在）；`python3 -c "from harness.drivers import CodexSdkDriver, CodexExecDriver; print('ok')"` OK；`python3 spikes/m0/conformance-second-impl.py` 10/10 Protocols + TrivialDriver driver 部分无回归；`spikes/m0/egress-httpx-actual.py` 8/8 无回归；`pip install -e .` OK |
| **T-TG-5** | ✅ done 2026-08-31 | `harness/testing/{__init__,echo_server}.py` — `InProcessEgressServer`（stdlib `ThreadingHTTPServer` + daemon thread；零新依赖）；host **hardcoded `127.0.0.1`**（fixture 性质，不是生产 egress；BLOCKED_NETWORKS 不动）；`port=0` → ephemeral（多次 `with` 不撞）；`__enter__` 起 thread / `__exit__` `shutdown()`+`server_close()`+`join(2.0)`；GET/POST `/echo` 200（POST body 原样回显 = 等价契约）；post-exit 连拒（`httpx.ConnectError`）；5-phase dedicated smoke (`python3 -m harness.testing.echo_server`) 全过；`from harness.testing import InProcessEgressServer` OK；egress 8/8 + conformance 10/10 无回归；`pip install -e .` OK |
| **T-DO-1** | ✅ done 2026-08-31 | `Dockerfile` (python:3.12-slim base, `PYTHONDONTWRITEBYTECODE=1` + `PYTHONUNBUFFERED=1`, `WORKDIR /app`, COPY `pyproject.toml`+`README.md` → COPY `harness/` → `pip install --no-cache-dir --no-compile .` → `CMD ["python", "-m", "harness"]`)；`harness/__main__.py`（3 行: `print(harness.__version__)`）；`README.md` placeholder（标 T-DD-1 会重写）。`docker build -t fish-harness:1.0.0a0 .` OK → image `0df04ebb91c6` (212MB)；`docker run --rm fish-harness:1.0.0a0 python -c "import harness; print(harness.__version__)"` → `1.0.0a0`；`docker run --rm fish-harness:1.0.0a0 python -m harness` → `1.0.0a0` |
| **T-DO-2** | ✅ done 2026-08-31 | `Dockerfile` 补丁 (COPY `spec/` → `/app/spec` + `ENV PYTHONPATH=/app`)；`harness/runtime/__init__.py` re-export `SqliteWorkerPool` / `SqliteEventSink` / `SqliteContextManager`；`docker-compose.yml` 含 `harness` + `test-runner` services + `harness_db` named volume + spike suite gate command。**base image 偏离 plan §2 T-DO-1 (3.12-slim → 3.14-alpine)** per [`docs/ADJUDICATION-sqlite-raise-T-DO-2.md`](ADJUDICATION-sqlite-raise-T-DO-2.md) (Accepted) — `spec/kernel-schema.sql` 的 `RAISE(ABORT, expr || expr)` 是 SQLite 3.47+ 才支持；3.12/3.13/3.14-slim + trixie 全 ship 3.46.1 → schema apply 失败；3.14-alpine ship 3.53.2 ✓。Dockerfile 加硬门 `assert sqlite3.sqlite_version >= 3.47.0`。验收 5/5: `import harness` 1.0.0a0; `from harness.runtime import …`; `from harness.gateway import …`; `from spec.interfaces…`; spike suite (conformance 10/10 + egress 8/8 + worker-dispatch 21/21 + worker-events 6/6 + context-budget 全过) **容器内** 全绿。Image `945685d5f836` **87.3MB** (vs 3.12-slim 212MB, alpine 瘦 59%)。注: 宿主 `docker compose` v2 plugin 未装, 用等价 `docker run` + 同 volume mounts 模拟 test-runner 行为 |
| **T-DO-3** | ✅ done 2026-08-31 | `.dockerignore` (13 patterns per DISPATCH-T-DO-3 §A): VCS/agent dirs (`.git/`/`.github/`/`.cursor/`/`.serena/`), spec/docs/notes (`adr/`/`notes/`/`docs/`), tests (`spikes/`/`tests/`), py caches (`__pycache__/`/`*.pyc`/`.pytest_cache/`), planning files (`PRD*.md`/`ARCHITECT*.md`/`RESPONSE*.md`), DB (`*.sqlite` + `-journal`/`-wal`/`-shm`), `uploads/`. 保留: Dockerfile/pyproject.toml/README.md/harness/spec. Build context **457.2kB** (vs 之前 ~40-60MB 无 ignore 状态 — 100x reduction). `docker build` 全部 layer cache HIT (image sha 未变). 容器 `from harness.runtime import ...` + `from harness.gateway import ...` + `sqlite3.sqlite_version >= 3.47.0` 全 PASS. |
| **T-DO-4** | ✅ done 2026-08-31 (interim smoke) | `.github/workflows/deploy.yml` (97 行): trigger = `push.tags=v*` + `workflow_dispatch`; concurrency group = `deploy-${{ github.ref }}`; 3 jobs: **(1) build** (docker/build-push-action@v5 + `load:true` 给 smoke 复用) → **(2) push** (GHCR login + 推 `${GITHUB_REPOSITORY_OWNER,,}/fish-harness:${tag}` + `:latest`; gated `if: push && startsWith(ref, 'refs/tags/v')`) → **(3) smoke** (容器内 5-spike subset bind-mount `spikes/` ro + harness-smoke-db vol; **interim gate** with TODO marker 等 T-QA-1 mutation_suite 替换). YAML valid via `python3 -c "import yaml; yaml.safe_load(...)"` (3 jobs, 2 triggers). 本机 rehearsal: 5/5 spike 全绿 (conformance 10/10 + egress 8/8 + worker-dispatch 21/21 + worker-events 6/6 + context-budget 全过). **不**真实打 `v1.0.0a0` 推 GHCR (无 token, 留用户/CI). 验收 5/5: `test -f .dockerignore` + `docker build` + 容器 imports + `test -f .github/workflows/deploy.yml` + YAML valid + 本机 smoke 5-spike 全过. |
| **T-DO-3+4 计划脚注** | ✅ done 2026-08-31 | `docs/v1.0-ga-team-plan.md` §2 T-DO-1 行加 ¹ 脚注 → base=`python:3.14-alpine`(或 3.12-alpine); `sqlite3.sqlite_version >= 3.47.0` 硬门 (schema 用 RAISE(expr)); 裁定全文 `docs/ADJUDICATION-sqlite-raise-T-DO-2.md`. §2 末尾加 ¹ 脚注定义块. plan 表文字未改 (base 仍写 3.12-slim + 脚注解释偏离). |
| **T-QA-1** | ✅ done 2026-08-31 | **Phase 0 (deploy P0 修)**: `.github/workflows/deploy.yml` smoke job 删除; smoke 步骤挪进 `build` job 末尾同 runner (image 在 build runner daemon 内 `load:true`, 跨 job 必挂 P0 修). YAML valid; jobs = build + push. **Phase A (mutation_suite lift)**: `harness/testing/mutation_suite.py` (1029 行 lift 自 `spikes/m0/mutation-test.py` v0.9.4) — `MUTATIONS` dict + `run_mutations() -> dict[str, bool]` + `main()` CLI. 17 mutations (M1-M11, M13-M18, M12 删). 路径优先 `harness.runtime._db/workers/context` (T-BE-1 lift); `seed_blob` 内联 (_helpers 未 lift, 1-stmt INSERT); M15 monkey-patch 改 target 到 `harness.runtime.workers.dispatch_worker` (production 路径, 非 spike _helpers). `harness/testing/__init__.py` 用 `__getattr__` lazy import (避免 Python 3.14 RuntimeWarning sys.modules pollution). 验收: `python3 -m harness.testing.mutation_suite` exit 0 host 17/17 + container 17/17 (无 spikes bind-mount 需要, 全用 production 路径); deploy.yml YAML valid; smoke 同 runner. **TODO 移除**: deploy smoke 不再是 interim 5-spike — 现在是正式 mutation_suite gate. |
| **T-QA-2** | ✅ done 2026-08-31 | **集成测试套件** (GA plan §2): 5 文件全在 `tests/` 下 (conftest.py + 4 test_*.py). `conftest.py` 4 fixtures: `conn` (per-test `make_db()`) + `artifact_root` + `upload_root` + `attempt` / `attempt_factory` (走 `harness.runtime._db.claim()` 拿真 attempt row 满足 `context_snapshots.attempt_id` FK + `trg_attempt_fence_insert`). `pyproject.toml` 加 `[tool.pytest.ini_options]` (testpaths=`tests`, filterwarnings=ignore PytestUnraisable). **37/37 PASS** in 0.24s: worker_pool 8 (register/dispatch×2 round-robin/heartbeat×2 monotonic/drain/claim_via_pool/Protocol/NoWorkerAvailable ⊂ WorkerPoolError); context_manager 8 (distill 幂等 + 缺 raw blob 报 ContextError + charge 累加 + 超 budget 报 BudgetExceeded + snapshot/restore 跨 terminal 新 attempt + restore 超 budget 报 BudgetExceeded + remaining/total + Protocol×2); egress 12 (PinnedResolver allowlist/private-IP-block/reject_rebinding + HttpEgressService 无 proxy 拒绝/MockTransport happy path/unlisted host 重定向/retry-on-ConnectError×2/backoff cap 8s); gateway 7 (allow 全 6 步 + deny 不调 provider + needs_approval 写 pending + bad lease/bad fence/no such attempt 全 step-1 拒绝 + provider 级 deny 透传). 验收: `pip install -e .` OK + `pytest tests/ -q` 37/37 + `python3 -m harness.testing.mutation_suite` 仍 17/17. 约束遵守: 0 处 import `spikes.m0._helpers`; egress 全 httpx.MockTransport (无真网); gateway 4 fake collaborator (FakePDP + FakeProvider + InMemoryArtifactStore[写 blobs 表满足 artifacts FK] + no linker). |
| **T-QA-3** | ✅ done 2026-08-31 | **benchmark 套件** (GA plan §2): `harness/benchmark/{__init__,runner}.py`. CLI `python -m harness.benchmark.runner --tasks=N --workers=N --out=path --csv=path` (defaults 50/4/results.json). `__init__.py` 用 PEP 562 `__getattr__` lazy import (避免 Python 3.14 sys.modules RuntimeWarning). 核心循环: `make_db()` (file DB) + `SqliteWorkerPool.register N` + `seed_task M` + 每 task 手写 `dispatch_worker + claim + terminal transition` (绕过 `claim_via_pool` 因其固定 `_now_iso(offset_seconds=15.0)` 在第 2 次同 worker 调用会触发 I16 backslide + 也会无谓改 last_heartbeat_at). I15 partial UNIQUE `idx_worker_one_active_attempt` 要求 worker 只能持 1 个 active attempt → 每 task 后 UPDATE attempt → succeeded + workers.current_attempt_id = NULL (不 bump last_heartbeat_at — I16 sub-millisecond ties 风险 + benchmark 不测 reap_stale). **硬门 p99 < 5000ms** exit 0, 否则 1. smoke `--tasks=10 --workers=2`: p99=0.43ms / 3695 tasks/s. 默认 `--tasks=50 --workers=4`: p99=1.49ms / 4128 tasks/s, **exit 0**. 输出 `results.json` 含 tasks/workers/wall_seconds/throughput_tasks_per_sec/latency_ms{p50,p95,p99,min,max,mean}/passes_gate/gate_threshold_ms. 无回归: `pytest tests/ -q` 37/37 + `python3 -m harness.testing.mutation_suite` 17/17. |
| **T-QA-4** | ✅ done 2026-09-01 | **3 个新 CI job** (`.github/workflows/ci.yml`, GA plan §2): **integration-tests** (py3.12+3.13 matrix, `pytest tests/ -v`) + **mutation-suite** (py3.12+3.13 matrix, `python -m harness.testing.mutation_suite`) + **benchmark-baseline** (py3.12, `if: github.event_name == 'workflow_dispatch'` 仅手动 — R5 资源护栏, `runner --tasks=50 --workers=4` + Python `passes_gate` 校验 + `actions/upload-artifact@v4` 上传 `results.json`). 3 job 独立无 `needs`; per-job `concurrency` group; `permissions: contents: read` 最小权限; `actions/setup-python@v5` + `cache: 'pip'` 自动 pip cache (key on pyproject.toml hash). YAML valid (`python3 -c "import yaml; yaml.safe_load(...)"`); 主机模拟 3/3 PASS (pytest 37/37 + mutation 17/17 + benchmark `passes_gate=true`). **deploy.yml + m0-contract-tests.yml + spikes/m0/_helpers.py + spec/kernel-schema.sql 全部未动**. Cursor 暂不可用, 架构师自签 PASS (`docs/REVIEW-T-QA-4.md`) — Cursor 复活后可追加签名. |
| **T-QA-5** | ✅ done 2026-09-01 | **SQLite WAL 并发压测** (GA plan §2 + §5 R-1/R-2): `harness/testing/stress_test.py` (374 行). CLI `python -m harness.testing.stress_test --workers=N --tasks=N --out=path --csv=path` (defaults 50/200/results.json). **每 thread 独立 sqlite3.Connection 共享 file DB + WAL mode** (`PRAGMA journal_mode=WAL` 在 seed + 每 thread conn 上 reaffirm). **Barrier(workers) peak-concurrency start**. **每 iteration 单 transaction** (BEGIN IMMEDIATE → claim (UPDATE task pending→claimed + INSERT attempt 'claimed') → succeed (UPDATE attempt claimed→succeeded + UPDATE task claimed→succeeded) → release worker → COMMIT). 关键抉择: 1) 单 transaction 消除 I15 race window (naive `_db.claim()` + outer auto-commit UPDATE 之间有微秒 gap, 同 worker 被另 thread dispatch → I15 partial UNIQUE 撞 ~1-2%); 2) 保留 'claimed' 中间态让 `trg_attempt_terminal_task_insert` trigger 通过 (直接 INSERT succeeded 撞 trigger 'cannot insert attempt for terminal task'); 3) 跳过 heartbeat bump 同 T-QA-3 benchmark; 4) distinct task_ids (10000) 避免同 task 双 claim。**硬门**: wall ≤ 60s + throughput ≥ 100/s + verification.all_match + 全计数器 = 0; 输出 `results.json` 含 workers/tasks_per_worker/total_attempts/wall_seconds/throughput_attempts_per_sec/latency_ms{mean,p50,p95,p99,max}/counters{claim_rejected,deadlocks,i15,i16,fk,unique,IntegrityError,...}/verification{all_match, task_attempts_total, succeeded, workers_active}/passes_gate/gate_threshold。**smoke 5×10**: wall=0.08s, 633/s, all_match=True. **full 50×200**: wall=5.08s, **1968/s** (R-2 阈值 100/s × 20 余量), p99=260ms, **all_match=True**, i15=0/fk=0/unique=0, exit 0. **无回归**: pytest 37/37 + mutation 17/17 + benchmark exit 0。**不进 ci.yml** per GA plan §5 R-5 资源护栏 (manual/diagnostic 工具). |
| **T-DD-1** | ✅ done 2026-09-01 | **README.md 重写** (GA plan §2 + §4 验证清单第 11 步): 覆盖 T-DO-1 placeholder (8 行 stub → 116 行 M3-grade). 9 sections: TL;DR + Architecture ASCII (5 框: gateway/WorkerPool/EventSink/ArtifactStore/Egress/ContextManager) + Quick start (`pip install -e .` + `import harness` + 4 命令 + 容器) + **10 Protocol 接口表** (WorkerPool/EventSink/ContextDistiller/ContextBudget/ContextManager/ArtifactStore/ToolInvocationGateway/ToolProvider/PolicyDecision/ExecutionDriver, 每行含 spec file + role + production impl) + **5 特性表** (WAL single-host 1968/s / trigger invariants I1/I11/I14/I15/I16/I17 / 6-step gateway / pinned-DNS egress / zero-CI-minute benchmark) + Tests+CI table (pytest 37/37 + mutation 17/17 + benchmark exit 0 + stress 10000/10000) + Container + CI (3 job + deploy.yml 引用) + Documentation index (7 link 到 NORTH-STAR/GA-plan/NOW/CC-POLL/§2/ADR-0008/ADR-0009 + 2 PENDING 标 CHANGELOG/LICENSE) + License footer (MIT + cscoheru). `pip show harness` Summary = `fish-harness v1.0 runtime (Python kernel) ...` (pyproject metadata); 9 sections ≥ 8 阈值。**无回归**: pytest 37/37 + mutation 17/17 + benchmark + stress 4/4 PASS. **pyproject.toml / harness/* / spec/* / .github/workflows/* / spikes/m0/_helpers.py 全部未动**. Cursor 暂不可用, 架构师自签 PASS (`docs/REVIEW-T-DD-1.md`) — Cursor 复活可追加签名. |
| **T-DD-2** | ✅ done 2026-09-01 | **CHANGELOG.md** (GA plan §2 + §4 step 11): 158 行 Keep-a-Changelog style. `## [v1.0.0a0] — 2026-09-01` 段含 **5 H3 块**: **Added** (5 subpackage + 10 Protocol + 3 runtime class + 3 gateway + 2 driver + echo server + container + 3 CI job + 4 suite + README) / **Changed** (Dockerfile base `python:3.12-slim → python:3.14-alpine` per ADJUDICATION T-DO-2; spike vs production 共存; deploy smoke 换 mutation_suite) / **Deprecated** (None — v1.0 first release) / **Security** (PinnedResolver SSRF / redirect re-pin / proxy-must-be-configured; audit_log 不可改不可删; RealArtifactStore sha256 校验) / **Fixed** (None — v1.0 baseline; v0.9.4 fixes 已 inherit 在 schema). **`## Upgrade path: v0.9 → v1.0`** section: spec baseline preserved + spike suite 共存 + container 新增 + `tests/` 新增; **"No data migration required"**. **`## ADR cross-reference`** table 9 行: 0001-0007 Accepted + 0008/0009 _pending T-DD-4/5_. README link `CHANGELOG.md` 现自动 resolve. `grep -E 'v1.0\|## '` 7 个 H2 + 5 个 H3 全识别. **无回归**: pytest 37/37 + mutation 17/17 + benchmark + stress 4/4 PASS. **README / LICENSE / pyproject / harness / spec / adr / CI / `_helpers.py` 全部未动**. GA plan §4 step 11 进度 4/5 (LICENSE pending T-DD-3). Cursor 暂不可用, 架构师自签 PASS (`docs/REVIEW-T-DD-2.md`). |
| **T-DD-3** | ✅ done 2026-09-01 | **LICENSE** (GA plan §2 + §4 step 11): 标准 MIT 21 行文本 (20 lines + final newline). `head -3 LICENSE` 输出 `MIT License` + (空行) + `Copyright (c) 2026 cscoheru`. 年份 2026 = CHANGELOG v1.0.0a0 release 同年; 版权人 cscoheru 与 pyproject `authors = [{name = "cscoheru"}]` + README §License footer 一致. **联动 PENDING 清理**: README Doc index line 119-120 (`CHANGELOG.md _pending T-DD-2_` + `LICENSE _pending T-DD-3 (MIT)_`) → `[CHANGELOG.md](CHANGELOG.md) v1.0.0a0 release notes; Keep-a-Changelog style. (T-DD-2)` + `[LICENSE](LICENSE) MIT; matches pyproject.toml license = {text = "MIT"} + authors = [{name = "cscoheru"}]. (T-DD-3)`; CHANGELOG `[Documentation]` 段 `_pending T-DD-3_` → 现 link (TDD-3). **README §License footer (line 122-124) 未动** (已正确). **pyproject.toml 未动** (`license = {text = "MIT"}` 已是 PEP 639 等价; 不需双标 SPDX `License-Expression`). cscoheru 4 文件一致 (pyproject=1 / README=2 / CHANGELOG=1 / LICENSE=1). **🎯 GA plan §4 step 11 = 5/5 ✅** (README + CHANGELOG + LICENSE + ADR 0008 + ADR 0009). **无回归**: pytest 37/37 + mutation 17/17 + benchmark + stress 4/4 PASS. **harness / spec / `_helpers.py` / CI / Dockerfile / 现有 ADR / poll doc 全部未动**. **NOTICE / AUTHORS 未建** (DISPATCH §已知 P1 — MIT 不带 NOTICE; 单人项目无需 AUTHORS). Cursor 暂不可用, 架构师自签 PASS (`docs/REVIEW-T-DD-3.md`). |
| **T-DD-4** | ✅ done 2026-09-01 | **ADR 0008 — v1.0 Package Architecture** (GA plan §2 + §4 step 12): 167 行新 ADR. Status=Accepted (v1.0) + Date=2026-09-01 + Deciders=Architect 自签. **Decision 段含**: (a) **5 subpackage table** (runtime/gateway/drivers/testing/benchmark 各列职责 + v0.9 spike ownership + 关键 production 类); (b) **4 条 binding 复用规则** (`spikes/m0/_helpers.py` + `conformance-second-impl.py` + `mutation-test.py` 不可删; 生产代码不反向依赖 spike); (c) **依赖方向图** runtime 无内部依赖 / gateway→runtime / drivers→runtime / testing→{runtime,gateway,drivers} / benchmark→{runtime,gateway}; (d) **4 类反向依赖禁令**; (e) **版本对齐规则** (pyproject version == CHANGELOG 段; spec 改动必须 sync 改 production + spike); (f) **10 Protocol 出口** (`from harness.spec_interfaces import (WorkerPool, EventSink, ContextDistiller, ContextBudget, ContextManager, ArtifactStore, ToolInvocationGateway, ToolProvider, PolicyDecision, ExecutionDriver)`; WorkflowPack data-only 不在出口范围). **Alternatives 4 个**: A1 单包 / A2 删 spike (用户硬规则禁) / A3 spike 引用 harness (失去 reference 角色) / A4 6 subpackage (与 spec/interfaces 重复). **联动**: CHANGELOG ADR table line 209 `_pending T-DD-4_` → `[ADR 0008](adr/0008-v1.0-package-architecture.md) Accepted (v1.0) documents harness/ 5-subpackage layout + spike→production ownership` (README T-DD-1 时已 link; 现 resolve). **无回归**: pytest 37/37 + mutation 17/17 + benchmark + stress 4/4 PASS. **harness / spec / `_helpers.py` / CI / pyproject / Dockerfile / 现有 ADR 0001-0007 / poll doc 全部未动**. **GA plan §4 step 12 进度**: 3/9 → **8/9 ✅** (剩 ADR 0009 待 T-DD-5). Cursor 暂不可用, 架构师自签 PASS (`docs/REVIEW-T-DD-4.md`). |
| **T-DD-5** | ✅ done 2026-09-01 | **ADR 0009 — SQLite WAL Production Constraints** (GA plan §2 + §4 step 12 最后一个新建): 132 行新 ADR. Status=Accepted (v1.0) + Date=2026-09-01 + Deciders=Architect 自签. **Decision 段含**: (a) **单 host 强约束 (4 bullet)**: ✅ SQLite WAL 单 host; ❌ NFS/CIFS/shared block storage (POSIX advisory lock 失效); ❌ multi-region active-active (Litestream 灾备 warm standby only); ❌ multi-host active-active (每 host 必须独立 harness_db file); (c) **docker-compose / production 约束**: local volume only + 单 host ≤ 16 worker (per GA plan §5 R-2 lock contention 上限); (d) **性能 + 并发上限**: 实测 baseline 1968/s + p99 < 70ms + BUSY_TIMEOUT 5000ms (T-QA-5 50×200 stress test); (e) **post-v1.0 评估路径 (NOT in v1.0)**: rqlite (multi-region 写一致) / Litestream (warm standby + ≤ 5min RPO) / rqlite+Litestream 组合; (f) **ADR 0008 ownership**: `harness/runtime/` 是 ADR 0009 implementation owner (per-connection re-affirm PRAGMA journal_mode = WAL); (g) **与 ADR 0005/0006/0007 关系**: 全部 single-host 假设. **Alternatives 4 个**: A1 NFS / A2 rqlite v1.0 (时间线不允许) / A3 Litestream v1.0 (灾备 SLA 未要求) / A4 PostgreSQL (schema 重写 + 全部 trigger 重写; 不 v1.0 scope). **联动**: CHANGELOG ADR table line 210 `_pending T-DD-5_` → `[ADR 0009](adr/0009-sqlite-wal-production-constraints.md) Accepted (v1.0) documents WAL single-host rule + multi-host/region NOT + post-v1.0 rqlite/Litestream evaluation path`; docker-compose.yml line 12 已引用 ADR 0009 (T-DO-2 时已加; 现自动 resolve); README line 118 已 link (T-DD-1; 现 resolve). **无回归**: pytest 37/37 + mutation 17/17 + benchmark + stress 4/4 PASS. **harness / spec / `_helpers.py` / CI / pyproject / Dockerfile / compose 注释 / 现有 ADR 0001-0008 / poll doc 全部未动**. **🎯 GA plan §4 step 12 = 9/9 ✅** (0001-0009 全部存在). Cursor 暂不可用, 架构师自签 PASS (`docs/REVIEW-T-DD-5.md`). |
| **T-DD-6** | ✅ done 2026-09-01 | **9 ADR v1.0 Status Footer Sync** (GA plan §2 收尾): 全部 9 个 ADR (`adr/0001-0009.md`) 文末 append 统一 footer 块 (16 行; 含 `**v1.0 Status: Included in GA** — 2026-09-01` + 冻结规则 3 bullet + 3 cross-ref link (CHANGELOG + ADR 0008 + ADR 0009)). **冻结规则**: 新增 ADR 编号 ≥ 0010; 不修改本 ADR 内容 (保留 v1.0.0a0 历史快照); 引用本文时用 `<adr-XXXX>` tag. **原始 Date 不动** (0001-0007 原创作日期 2026-08-29/30 + 0008/0009 2026-09-01 保留; 避免 falsify history). **ADR 0009 placeholder 清理**: 原 "v1.0 Status Footer (T-DD-6 同步加)" placeholder 段 (5 行) 已删, 避免与新 footer 重复; `grep -c "v1.0 Status Footer (T-DD-6" adr/0009` = 0. **联动 cross-ref**: ADR 0001-0007 通过 footer 引 0008/0009 (原 Related 段不动); 0008/0009 footer self-ref 自然保留. **9/9 验证 PASS**: per-ADR `grep -c "v1.0 Status: Included in GA"` = 1; ADR 0008 link = 9/9 (0009 有 2 = Related + footer); ADR 0009 link = 9/9. **无回归**: pytest 37/37 in 0.28s + mutation 17/17 + benchmark exit 0 + stress 5×10 exit 0. **harness / spec / `_helpers.py` / CI / pyproject / Dockerfile / compose / LICENSE / README / CHANGELOG / 现有 ADR 9 文件 front-matter (Status / Date / Context / Decision / Alternatives / Consequences) 全部未动**. **🎯 GA plan §4 12 步 = 12/12 ✅** + **9 ADR v1.0 Status footer = 9/9 ✅** = **M3 Exit Gate 全绿**. Cursor 暂不可用, 架构师自签 PASS (`docs/REVIEW-T-DD-6.md`). **Surface 4 用户裁断点**: (1) v1.0.0 GA tag 不可逆; (2) T-DO-5 codex-review gate 状态 (deferred); (3) Cursor review backfill 5+ tasks; (4) M3 Exit Gate sign-off. |
| **T-M3-SIGNOFF** | ✅ done 2026-09-01 | **v1.0.0a0 GA tag execution + 5th corrective commit** (post-T-DD-6, per user裁断): (a) **pre-tag audit** found HEAD `1dc9d0b` missing 12 harness/ files (runtime lift + gateway lift + `__init__.py` + `__main__.py`) — would have tagged a broken release where `pip install -e . && python -c "import harness"` → ModuleNotFoundError. Surfaced as 5th user-decision point. (b) user chose **Option A** = commit untracked then tag. (c) **`feat(runtime)` commit `dc9d61b`**: 12 files / +1996 LOC / 0 deletions. T-BE-1 (`harness/runtime/{_db,workers,context}.py` 499 lines) + T-BE-2 (`worker_pool.py` 122 lines SqliteWorkerPool) + T-BE-3 (`event_sink.py` 84 lines SqliteEventSink 4 events) + T-BE-4 (`context_manager.py` 328 lines SqliteContextManager) + T-BE-5 (`__init__.py` + `__main__.py` 84 lines 10 Protocol 出口) + T-TG-1 (`gateway/egress.py` 215 lines HttpEgressService + PinnedResolver + BLOCKED_NETWORKS) + T-TG-2 (`gateway/gateway.py` 256 lines ToolInvocationGatewayImpl 6-step 链) + T-TG-3 (`gateway/artifact_store.py` 361 lines RealArtifactStore) + T-TG-5 (`gateway/__init__.py` 47 lines). (d) `git push origin main` = `1dc9d0b..dc9d61b`. (e) **GA plan §4 全 12 步 re-verify on HEAD `dc9d61b`** = 12/12 ✅: step 1 sqlite3 schema apply OK; step 2 `pip install -e . + import harness` → `harness.__version__=1.0.0a0`; step 3 `from harness.runtime import SqliteWorkerPool,SqliteEventSink,SqliteContextManager` OK; step 4 `from harness.gateway import HttpEgressService,ToolInvocationGatewayImpl,RealArtifactStore` OK; step 5 5 core spikes all green (conformance-second-impl / worker-dispatch-test 21 cases / worker-events-emit-test 6 cases / context-budget-test NULL-budget unlimited / mutation-test 17 reverse-DROP); step 6 `docker build -t fish-harness:1.0.0a0 .` → `Successfully built de55a7f34fd0`; step 7 `docker run --rm fish-harness:1.0.0a0 python -c "import harness"` → `1.0.0a0`; step 8 pytest 37/37 PASS in 0.28s; step 9 mutation_suite 17/17 PASS; step 10 benchmark `passes_gate=true`; step 11 5 files all exist; step 12 9 ADR + 9 v1.0 Status footer. (g) **`git tag -a v1.0.0a0 -m "v1.0.0a0 release — fish-harness M3 Exit Gate 12/12 ✅"`** (irreversible). (h) `git push --tags` = `[new tag] v1.0.0a0 -> v1.0.0a0`. (i) post-tag verification: tag object `9ac508a` → commit `dc9d61b` = HEAD (`git rev-parse v1.0.0a0^{commit}` = `dc9d61b`); `git ls-remote origin v1.0.0a0` = `9ac508a...` (remote visible). **4 user裁断 points ALL applied**: (1) tag GO — executed; (2) codex-review gate (c) accept no PR review gate risk — T-DO-5 not creating codex-review.yml per accepted risk; (3) Cursor review backfill (x) auto-accept signatures — 8 architect self-signed REVIEW files remain in place; Codex 5 PM revival batch-review pending; (4) M3 Exit Gate sign-off ACCEPTED. **NOT touched** (per scope discipline): docs/poll/* + docs/{DISPATCH,REVIEW}-T-*.md + notes/ + .cursor/ + .serena/ + docs/HANDOFF-TO-CURSOR.md (modified, uncommitted) + results.json (modified, uncommitted) — poll infra / ephemeral artefacts, not v1.0.0a0 release deliverables. fish-harness v1.0.0a0 GA complete. cc-ready dropped at `docs/poll/cc-ready.json` task_id=T-M3-SIGNOFF. |
| **T-GH-RELEASE** | ✅ done 2026-09-01 | **GitHub Release page created for v1.0.0a0** (per user裁断 "创建 GitHub Release page"): (a) `gh --version` = 2.86.0; `gh auth status` = cscoheru authenticated via keyring. (b) Extracted CHANGELOG.md lines 10-148 (139 lines) as `/tmp/release-notes.md` — full [v1.0.0a0] 段含 5 H3 块 (Added/Changed/Deprecated/Security/Fixed) + ADR cross-ref + Upgrade path链接. (c) Initial `gh release create --target dc9d61b` (短 SHA) 失败 HTTP 422 "Release.target_commitish is invalid" — 改用 full SHA `dc9d61b29ae478873708d72dd565498ed3e49804`. (d) `gh release create v1.0.0a0 --target <full SHA> --title "v1.0.0a0 — fish-harness M3 Exit Gate 12/12 ✅" --notes-file /tmp/release-notes.md --verify-tag` = URL `https://github.com/cscoheru/harness/releases/tag/v1.0.0a0`. (e) `gh release view v1.0.0a0` 验证: title / tag / author=cscoheru / created=2026-09-01T05:03:47Z / **published=2026-09-01T05:48:46Z** / 完整 139 行 markdown notes. (f) `gh release list` = "Latest v1.0.0a0 — fish-harness M3 Exit Gate 12/12 ✅". **fish-harness v1.0.0a0 现在 GitHub 上有正式 Release page + git tag + immutable commit 引用**. cc-ready dropped at `docs/poll/cc-ready.json` task_id=T-GH-RELEASE. |
| **T-POST-M3-POLISH** | ✅ done 2026-09-01 | **Post-M3 polish per gap analysis** (per user "继续 post-M3 polish"): (a) **VISION line 9 fix** — `docs/VISION-v1.0-supplement.md` line 9 消除"另议"歧义; 改写为 "鱼鳞(runtime)=v1.0.0a0 DONE 2026-09-01 (tag→dc9d61b)" + "鱼之(product)=v1.1+ scope 不在 v1.0.0a0". 关闭 gap analysis item A-25. (b) **`.gitignore` 扩展** — 新增 `.cursor/` `.serena/` `docs/poll/ACTIVE` `docs/poll/state.json` `docs/poll/ticks.log` (agent 工作目录 + ephemeral poll 状态 不在 v1.0.0a0 scope). (c) **Poll protocol 归档** — 13 untracked 文件提交: docs/CC-POLL.md + POLL-PROTOCOL.md (109 lines) + ADJUDICATION-sqlite-raise-T-DO-2.md (T-DO-2 SQLite 3.47 deviation 决策记录) + DISPATCH-T-DO-{2,3}.md + REVIEW-T-DO-{2,3}.md + DISPATCH-T-QA-{1,2,3}.md + REVIEW-T-QA-{1,2,3}.md + HANDOFF-TO-CURSOR.md (preserved with local state) + notes/codex-review-v0.9.5-report.md (Codex 28/28 反例 review) + results.json (benchmark output). (d) **Total**: 18 files / +843 LOC / -47 deletions. (e) **post-commit working tree clean** — only .cursor/.serena/docs/poll/* (all .gitignored). (f) v1.0.0a0 tag `9ac508a → dc9d61b` immutable (this polish commit 在 HEAD `1c3dc06` 之上 post-tag). cc-ready dropped at `docs/poll/cc-ready.json` task_id=T-POST-M3-POLISH. |
| **T-M3-FAILFIX** | ✅ done 2026-09-01 | **Codex 初次审验 6 FAIL 修复** (commit `47ba181`; per `notes/codex-review-v1.0.0a1-report.md` CHANGES REQUIRED): (a) **FAIL-1 major** — `harness/benchmark/__init__.py` PEP 562 `__getattr__` 用 `from . import runner` 触发 Python import system `hasattr` 无限递归 → 改 `importlib.import_module("harness.benchmark.runner")` 切断 `__getattr__("runner")` 自调用; (b) **FAIL-2/4** — `CHANGELOG.md` 新增 `[v1.0.0a1] — 2026-09-01` deploy-only patch 段, 说明 library 版本不变 + 21 文件 diff 范围不含 library 代码; (c) **FAIL-3** — `harness/__init__.py` 补 `ToolProvider` / `PolicyDecisionPoint` / `ExecutionDriver` 3 个 Protocol (5→10), `__all__` 同步, docstring 区分 `PolicyDecisionPoint` (Protocol) vs `PolicyDecision` (dataclass); (d) **FAIL-5** — `notes/codex-review-prompt-v1.0.0a1.md` prep 表改用实际 13 个 spike/ADR 文件路径 (而非 DISPATCH-T-DO-4 §C 模板占位符); (e) **FAIL-6** — ADR 0008 §10 Protocol 出口措辞 (从"仅接口类型"→具体 `harness.runtime/gateway/drivers` impl 列表) + compose image 1.0.0a0→1.0.0a1 + README 6 处 a0→a1 sync. **未动**: spec/ / spikes/ / Dockerfile / 现有 ADR body. cc-ready dropped at `docs/poll/cc-ready.json` task_id=T-M3-FAILFIX. |
| **T-M3-RECHECK-PASS** | ✅ done 2026-09-01 | **Codex 修复复审 PASS** (per `notes/codex-review-v1.0.0a1-recheck-report.md`): §2 FAIL 修复矩阵 9/9 ✅ + §3 回归 sanity 4/4 ✅ (pytest 37/37 + mutation 17/17 + benchmark passes_gate + conformance 10 Protocols) + §4 硬规则 5/5 ✅ + §5 范围隔离 10/10 精确落在白名单 (spec/spikes/Dockerfile/pyproject/ADR 0001-0007/0009 零触碰). 初次审验 6 FAIL (FAIL-1 major RecursionError + 5 minor) 全部由 commit `47ba181` 修复. Codex 顺手签发下一任务 = `docs/DISPATCH-T-V1-GA-TAG.md` = v1.0.0 GA tag 发布流程. 3 untracked 文件归档: `notes/codex-review-prompt-v1.0.0a1-recheck.md` (217 行) + `notes/codex-review-v1.0.0a1-recheck-report.md` (84 行) + `docs/DISPATCH-T-V1-GA-TAG.md` (68 行). cc-ready dropped at `docs/poll/cc-ready.json` task_id=T-M3-RECHECK-PASS. |
| **T-V1-GA-TAG** | ✅ done 2026-09-01 | **v1.0.0 GA tag 发布流程** (per user裁断 "1A, 2A" GO on tag + GHCR public): (a) **Phase A: 版本四元组 bump** — `pyproject.toml` `version = "1.0.0"` + `harness/__init__.py` `__version__ = "1.0.0"` + `docker-compose.yml` 2× `image: fish-harness:1.0.0` + `README.md` 8 处 1.0.0a0/a1→1.0.0 + `CHANGELOG.md` 新增 `[1.0.0] — 2026-09-01` GA 段 (含 a1 复审修复摘述 + 版本对齐声明 + GA plan §6 M3 Exit Gate complete acknowledgment). commit `ab8749a` 5 files / +62 / -14. (b) **Phase B: GA plan §4 全 12 步在 release commit 复跑** = 12/12 ✅ (step 1 sqlite3 schema apply OK + step 2 import harness 1.0.0 + step 3 runtime imports + step 4 gateway imports + step 5 5 core spikes all green + step 6 docker build 32db8e1f16d6 OK + step 7 docker run import 1.0.0 OK + step 8 pytest 37/37 in 0.26s + step 9 mutation_suite 17/17 PASS + step 10 benchmark p99=0.41ms passes_gate=true + step 11 5 files all exist + step 12 9 ADR). 附加 FAIL-1 regression `python3 -c "from harness.benchmark import runner"` exit 0. (c) **Phase C: tag + push + deploy + Release** — `git tag -a v1.0.0 -m "..."` (annotation 含复审证据 + release commit + 用户裁断引用) → `git push origin v1.0.0` (via Clash proxy) → deploy.yml run `33494120237` 启动 → build+smoke job success (09:48:18Z) + push job success (09:48:51Z) → GHCR image digest `sha256:4e3d077d407f026ccc2940d0d512886fb15e4e02eb47f38f0c2c5f75e313bc62` + config digest `sha256:dd68fc0b8f4836025a67013abfa7e13d09aed27ab75e6c6c6df036c773870497` → `gh release create v1.0.0 --target ab8749a2dde1714f268c6271387fafb6f2c800a6 --title "v1.0.0 — fish-harness M3 Exit Gate complete" --notes-file <CHANGELOG [1.0.0] 段>` → URL `https://github.com/cscoheru/harness/releases/tag/v1.0.0`. (d) **GHCR public verified by user** — 用户将 GitHub repo cscoheru/harness 翻 public, GHCR package cscoheru/fish-harness (本地 dir 名 + deploy.yml image 名) 同步翻 public. 验证证据: docker pull ghcr.io/cscoheru/fish-harness:v1.0.0 (no auth) → Digest sha256:4e3d077d... 完全匹配 deploy push (92.1MB, 57 分钟前 created); docker run 容器内 harness.__version__=1.0.0. 我之前 URL 报错根因 = package 名是 fish-harness 不是 harness. (e) doc 收尾 — NOW.md §2/§4 + cc-ready.json BLOCKER 标注移除 + 加 ghcr_visibility=public 字段. cc-ready dropped at `docs/poll/cc-ready.json` task_id=T-V1-GA-TAG. |

任务全文：`docs/v1.0-ga-team-plan.md` §2。派发顺序：§8（**T-BE-5 最先**）。

## 3. 禁止（本阶段）

- dsh、TypeScript wrapper、PWA、STT、Web Push、6 host、工作流 B/C
- 引用 VISION 正文作条款
- 跨 host SQLite / NFS
- 扩大范围到当前「下一枪」以外（做完一枪即停）；T-TG-2 可并行但不自动开

## 4. 下一验收（M3 Exit Gate 终态 + 用户裁断点）

见 [`docs/DISPATCH-T-DD-6.md`](DISPATCH-T-DD-6.md)（如已签发；否则等 Cursor 复活 / 下条用户信号）。  
审验：[`docs/REVIEW-T-DD-5.md`](REVIEW-T-DD-5.md) = **PASS**（架构师自签 — Cursor 暂不可用，ADR 0009 132 行 + WAL single-host + 性能 baseline 1968/s + post-v1.0 评估路径；GA plan §4 step 12 = 9/9 ✅🎯；无回归 4/4 PASS）。

审验：[`docs/REVIEW-T-DD-6.md`](REVIEW-T-DD-6.md) = **PASS**（架构师自签 — Cursor 暂不可用；9 ADR v1.0 footer + ADR 0009 dedup；无回归 4/4 PASS）。

**T-M3-SIGNOFF ✅ DONE 2026-09-01**（详见 §2 T-M3-SIGNOFF row + `docs/poll/cc-ready.json` task_id=`T-M3-SIGNOFF`）：

- **5th user-decision point (pre-tag audit)** — HEAD `1dc9d0b` 缺 12 个 harness/ 文件（runtime lift + gateway lift + `__init__.py` + `__main__.py`）；用户选 **Option A** = 先 commit 后 tag。
- **Corrective commit `dc9d61b`**：`feat(runtime): T-BE-1..5 + T-TG-1..5 production runtime + gateway lift`（12 files / +1996 LOC）已 push `origin main`。
- **GA plan §4 全 12 步在 HEAD `dc9d61b` re-verify** = 12/12 ✅（含 pip install + 3 个 import + 5 spike + docker build + docker run import + pytest 37/37 + mutation 17/17 + benchmark passes_gate + 5 文件存在 + 9 ADR + 9 v1.0 Status footer）。
- **`git tag -a v1.0.0a0` + `git push --tags`**（不可逆）— tag object `9ac508a` → commit `dc9d61b` = HEAD；`git ls-remote origin v1.0.0a0` 远程可见。
- **4 用户裁断点 ALL applied**：(1) tag GO 执行 ✅；(2) codex-review gate (c) 接受无 PR gate 风险 ✅；(3) Cursor review backfill (x) 自动接受签名 ✅；(4) M3 Exit Gate sign-off ACCEPTED ✅。

**🎯 fish-harness v1.0.0a0 GA COMPLETE 2026-09-01**。M3 Exit Gate 12/12 + 9 ADR footer 9/9 + runtime + gateway + tag = end-of-M3。下一验收为空（新工作必须开 v1.1+ 周期，从 ADR 编号 ≥ 0010 起；不在 v1.0 scope 内）。

**T-GH-RELEASE ✅ DONE 2026-09-01**（详见 §2 T-GH-RELEASE row + `docs/poll/cc-ready.json` task_id=`T-GH-RELEASE`）：GitHub Release page created for v1.0.0a0 at `https://github.com/cscoheru/harness/releases/tag/v1.0.0a0` (published 2026-09-01T05:48:46Z, full CHANGELOG [v1.0.0a0] 段 + 5 H3 块 as release notes)。

**T-POST-M3-POLISH ✅ DONE 2026-09-01**（详见 §2 T-POST-M3-POLISH row + `docs/poll/cc-ready.json` task_id=`T-POST-M3-POLISH`）：4 项变更打包：VISION line 9 消除"另议"歧义 + `.gitignore` 加 agent/poll state 排除 + 13 file poll protocol 归档 + working tree 全清。关闭 gap analysis item A-25。

**T-M3-FAILFIX ✅ DONE 2026-09-01**（详见 §2 T-M3-FAILFIX row + `docs/poll/cc-ready.json` task_id=`T-M3-FAILFIX`）：Codex 初次审验 6 FAIL (FAIL-1 major RecursionError + 5 minor) 全部由 commit `47ba181` 修复 — importlib.import_module 切断 PEP 562 自调用 + CHANGELOG a1 段 + harness 10 Protocols + prep 表 13 文件 + ADR 0008 措辞 + compose/README 版本对齐。library code zero-drift (spec/spikes/Dockerfile/现有 ADR body 未动)。

**T-M3-RECHECK-PASS ✅ DONE 2026-09-01**（详见 §2 T-M3-RECHECK-PASS row + `docs/poll/cc-ready.json` task_id=`T-M3-RECHECK-PASS`）：Codex 修复复审 PASS — §2 FAIL 修复矩阵 9/9 ✅ + §3 回归 4/4 ✅ + §4 硬规则 5/5 ✅ + §5 范围隔离 10/10 ✅。Codex 签发下一任务 = `docs/DISPATCH-T-V1-GA-TAG.md`。

**T-V1-GA-TAG ✅ DONE 2026-09-01**（详见 §2 T-V1-GA-TAG row + `docs/poll/cc-ready.json` task_id=`T-V1-GA-TAG`）：v1.0.0 GA tag 发布流程全 5 phase (A 版本四元组 bump + B §4 12 步复跑 12/12 ✅ + C tag push → deploy run 33494120237 success → GHCR digest `4e3d077d...` → GitHub Release page + D **GHCR public verified by user** → E doc 收尾) 100% 完成。release commit `ab8749a` on main, GHCR `cscoheru/fish-harness:v1.0.0` digest `sha256:4e3d077d...` 免认证 pull 验证 + 容器内 `harness.__version__=1.0.0`。**GHCR 可见性 = public**（用户把 repo 翻 public, GHCR package 同步翻 public; 我之前 URL 报错的根因 = package 名是 `fish-harness` 不是 `harness`）。下一验收为空 (v1.0 周期关闭；新工作开 v1.1+ 周期, ADR ≥ 0010)。

## 5. 冷指针（按需 Read，勿预载）

| 要干什么 | 读 |
|----------|----|
| 任务/验收/角色 | `docs/v1.0-ga-team-plan.md` |
| v1.0 豁免哪些守门 | `docs/PRD-V0.1-NORTH-STAR.md` §13 |
| 产品愿景 / v1.1（未开工） | `docs/PRD-v1.1-product.md`（讨论稿） |
| 六项裁定 | `docs/DOCS-REVIEW-v1.1-adjudication.md` §3 |
| Start 门 | `docs/DOCS-REVIEW-v1.0-start-gate.md` |
| 接口 | `spec/interfaces/*.py`（10 Protocol） |
| schema / spike | `spec/kernel-schema.sql`；`spikes/m0/` |
| 归档（禁止当合同） | `docs/VISION-v1.0-supplement.md` |
