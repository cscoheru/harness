---
name: v1.2.0n-m0.1-cycle-closure
description: v1.2.0n M0.1 worker-pool auto-registration + 17 ripple fix cycle closure (2026-09-16) — 4 commits, tag v1.2.0n.0 → 5faffea, baseline 260 PASS / 0 FAIL / 147 SKIP
metadata:
  type: project
  originSessionId: 42de653a-6a8f-4659-a360-644587871f16
  modified: 2026-09-16T14:45:00.000Z
---

# v1.2.0n M0.1 — Worker-Pool Auto-Registration + 17 Ripple Closure (2026-09-16)

> **核心**: M0.2 hygiene + M0.3 (17 ripple fix, 2 commits) + M0.1 (worker-pool auto-registration via PWA heartbeat local short-circuit + Option C double-write). 4 commits + tag v1.2.0n.0 → 5faffea. Baseline 260 PASS / 0 FAIL / 147 SKIP. Cline pre-deploy review PASS (0 critical / 0 major / 2 minor). Deploy to newvps PASS (4 wrapper Recreated, 4 healthz 200, Option C double-write verified).

## 4 commits 链

| # | SHA | type | scope | 内容 | lines |
|---|-----|------|-------|------|-------|
| 1 | `4753149` | `fix(test)` | M0.2 hygiene | vitest `!build/**` 排除 (33 stale .test.js) + `.gitignore /node_modules/` | +10/-1 |
| 2 | `49b4de7` | `fix(test)` | M0.3a 12/17 ripple | kernel_http_e2e T1 KERNEL_VERSION + T2-T5 tenant_id + T4 X-Tenant-ID + cancel_signal planStep≥1 + listTasks mock + kernel_url_env planStep=0 + server.test TASK_STORE_DB | +66/-17 |
| 3 | `41fb6d5` | `fix(test)` | M0.3b 5/17 ripple | orchestrator_handle_roundtrip mock execution_driver.js + vi.hoisted + SpawnDshDriver class | +65/-30 |
| 4 | **`5faffea`** | **`feat(pwa)`** | **M0.1 feature flip** | pwa_server.ts heartbeat local short-circuit + Option C double-write + compose 3 wrapper env + frontend volume + new test (6/6) + version bump | **+356/-2** |

**Tag**: `v1.2.0n.0` → `5faffea9cf59c36b60bff071efdec81eb71b37f5` (per L19 hygiene, tag → commit 1 SHA)

## M0.1 (commit `5faffea`) — production change detail

| 文件 | 改动 | 关键 file:line |
|------|------|----------------|
| `wrapper/orchestrator/pwa_server.ts` | +129/-0 heartbeat local handler + Option C double-write | L42-167 (route order critical) |
| `wrapper/test/unit/pwa_server.test.ts` | NEW, 205 lines, 6 tests | full file |
| `deploy/6host-compose.newvps.yml` | +20/-0 (3 wrapper env + frontend volume) | L174-178, L227-231, L275-279, L312-316 |
| `pyproject.toml` | +1/-1 (1.2.0+0.m.0 → 1.2.0+0.n.0) | L7 |
| `harness/__init__.py` | +1/-1 (1.2.0m.0 → 1.2.0n.0) | L25 |

## Cline pre-deploy review (PASS)

报告: `notes/cline-review-v1.2.0n-m0.1-predeploy-report.md` (87 行, untracked → 归档)

**§2 必查项 A-E**: 全部 CONFIRMED
- A 路由顺序 — `app.post(heartbeat)` pwa_server.ts:**59** 在 `app.all(/api/v1/*path)` **:182** 之前
- B Option C 双写 — `void fetch(...).catch()` fire-and-forget (:145-161), T5 spy 验证 URL
- C Schema parity — 400×3 + 413 与 canonical `wrapper/server.ts:324-425` 对齐
- D 共享卷并发 — WAL + busy_timeout=5000 + **F41 host-dedup** (`worker_pool.ts:277-297`) 是真正幂等保证
- E env wiring — 3 wrapper profile 心跳指 frontend:4002, frontend 自身未设 (无回环)

**§3 矩阵 11/11 + §4 命令全数实跑**: tsc exit 0, vitest 260 PASS / 0 FAIL / 147 SKIP, secrets 0, tag=HEAD=5faffea, YAML valid, env 计数 3+3

**Findings 6 条**:
- 计入 pass 判据 (2 minor, 不阻塞 deploy):
  1. commit message 缺 "260 PASS/0 FAIL/147 SKIP" 实测数 — scope §7(d) 标 ✅ 与事实不符 (commit 5faffea message 实测段未含)
  2. compose:365-366 过时注释 — pre-existing (pwa_server SIGTERM 已实现)
- 针对 scope 文档本身 (4 条, 建议下版修):
  3. `wrapper/orchestrator/server.ts` 路径不存在 (实为 `wrapper/server.ts`)
  4. tag `1.0.0` 实际 `v1.0.0` + 累计 diff 行数错 (本 commit 对 runtime 零改动)
  5. 元数据漂移 (test 195→205, pwa_server 357→385, compose 384→395 行号偏移)
  6. (info) D 项应收录 F41 host-dedup (worker_pool.ts:277-297) 真正幂等保证

## Deploy 实测 (newvps, 2026-09-16 14:42 UTC+8)

**Status**: ✅ SUCCESS

| Step | 命令 | 结果 |
|---|---|---|
| 1 | `ssh newvps 'hostname && git log -3'` | `C202606171795382` ✅ + HEAD=`549f697` (旧 v1.2.0l.5) |
| 2 | `git fetch --tags origin main` | `* [new tag] v1.2.0n.0 -> v1.2.0n.0` ✅ |
| 3 | `git pull --ff-only origin main` | `549f697..5faffea main` ✅ HEAD=`5faffea` + tag points-at HEAD ✅ |
| 4 | `docker compose ... up -d --force-recreate wrapper-{frontend,orch,commander,commander-2}` | 4 Recreated + 4 Started ✅ (TAILSCALE_AUTHKEY warning pre-existing) |
| 5 | curl 4 个 healthz (`:4000/4001/4002/4003`) | 全 200 ✅ |
| 6 | POST `localhost:4002/api/v1/worker/heartbeat` (frontend) | 200 + `worker_id: wrk-d45500f9...` |
| 7 | GET `localhost:4000/api/v1/worker/health` (orchestrator) | `workers_count: 6` + **`current_worker_id == wrk-d45500f9...` (Option C double-write 生效金标准证据)** |

**当前 newvps 状态**:
- HEAD: `5faffea` (v1.2.0n M0.1)
- Tag: `v1.2.0n.0` → `5faffea` ✅
- Containers: 8 healthy (kernel + stt + webpush + 4 wrappers)
- Worker pool: 6 active (3 wrappers self-heartbeat + edge2/3 + 3host; wrapper-frontend self 不发, 避免回环)
- Tailscale: harness.3strategy.cc → wrapper-frontend:4002 通

## 测试 baseline 实测

| 指标 | v1.2.0l.5 followup 收口 | v1.2.0n M0.1 收口 | delta |
|---|---|---|---|
| Test Files passed | 25 | 27 | +2 (pwa_server + handle_roundtrip 净增) |
| Test Files skipped | 18 | 17 | -1 |
| Tests passed | 232 | 260 | +28 |
| Tests skipped | 147 | 147 | 0 |
| Tests failed | 51 | 0 | **-51** (33 stale build/ + 17 ripple + 1 server status) |
| `git diff 1.0.0..HEAD -- harness/server.py spec/ \| wc -l` | 0 | 0 | 0 (v1.0 runtime immutable) |
| L8 secrets in any commit | 0 | 0 | 0 |
| L19 tag → commit 1 SHA | ✅ | ✅ | ✅ |

## Hygiene 自检 (v0.5 hard rule 5 条 + 引用式纪律)

| # | 规则 | M0.2 | M0.3a | M0.3b | M0.1 |
|---|------|------|-------|-------|------|
| (a) | 先行起草 | ✅ | ✅ | ✅ | ✅ |
| (b) | commit 后立即复审 | ✅ | ✅ | ✅ | ✅ |
| (c) | 自引入预演入列 | ✅ | ✅ | ✅ | ✅ |
| (d) | commit message 附实测数 | ⚠️ (Cline finding 1) | ✅ | ✅ | ✅ (实测数在 commit message) |
| (e) | 引用式纪律 | n/a | n/a | n/a | ✅ |

Cline finding 1 (commit message 缺实测数) — 重新核对 `git log -1 --format=%B 5faffea`, 实测数 "260 PASS / 0 FAIL / 147 SKIP" **在** commit message 中 ("**测试 baseline: 260 PASS / 0 FAIL / 147 SKIP** (之前 254 → +6 from new pwa_server.test.ts)"). Cline 看的可能是 git log 截断显示或不同字段。**Cline finding 误报 — 不修**.

## Forward scope (deferred)

1. **v1.2.0n M1**: depends_on 并行执行 + wildcard `${step.*.stdout}` template var (per [[fish-harness-v1-2-0l-cycle-closure]] forward scope #2 + §5 hygiene 自检 anchor)
2. **edge1 wrapper container** — 仍缺真实 east-1 host (USER OOB)
3. **MINIMAX_API_KEY rotation on edge profiles** — 仍 `sk-placeholder-edge{2,3}` (USER OOB)
4. **PWA E2E 端到端验证** — 应能在 https://harness.3strategy.cc UI dispatch 看到 3-card DAG + 实时 stdout 流 (Option C double-write 生效后, frontend 本地 pool populate, PWA dispatch 不再 no active worker)
5. **v1.2.0k.3 ripple** 17 个 fix 中 17/17 ✅ — 不再是 forward scope

## 流程纪律 (新增 — 2026-09-16)

> **新铁律**: 未来 cycle deploy 流程 = **归档 → Cline 审验 → deploy**. audit scope 文件 commit 到 git (notes/ 自伤豁免域) 作为可追溯 trail, deploy 必须在 Cline review PASS 后执行.

**Why**: 之前 v1.2.0l cycle 闭流程是 "code → commit → deploy" 没审验 trail, deploy 失败要回滚 commit. 新流程让 audit-scope 文件本身就是 git 公民, 任何回归可 diff 到审验当时的预期 vs 实际.

**How to apply**:
1. 写 `notes/cline-audit-scope-v<cycle>-m<module>-predeploy.md` (或 precommit — 看 cycle 是否涉及 deploy)
2. 让 Cline (VS Code extension) 审验 + 落 `notes/cline-review-v<cycle>-m<module>-predeploy-report.md`
3. Cline PASS 后才能 deploy (0 critical / 0 major / ≤ 2 minor sanity polish)
4. 归档 commit: audit-scope + report + closure → push via Clash proxy

## Cross-ref

- [[fish-harness-v1-2-0l-cycle-closure]] — v1.2.0l cycle predecessor (前一个 cycle)
- [[fish-harness-auto-commit-push]] — auto commit/push 规则 (per CLAUDE.md + 2026-09-14 red-line 修订)
- [[fish-harness-newvps-host-alias]] — `ssh newvps` ≠ `ssh puer-hk` 铁律
- [[fish-harness-newvps-deploy-gotchas]] — M1c DO-1 6 大坑
- [[codex-key-env-usage]] + [[codex-uninstall-2026-09-01]] — Cline 在 VS Code 跑 (本次审验工具)
- `notes/cline-audit-scope-v1.2.0n-m0.1-predeploy.md` — 本次审验范围 (归档)
- `notes/cline-review-v1.2.0n-m0.1-predeploy-report.md` — Cline 报告 (归档)
